---
phase: 01-dependency-upgrade
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified: []
autonomous: false
requirements: [UPG-03]
user_setup:
  - service: k3s deploy (deploy.sh via Helm)
    why: "deploy.sh passes these to Helm as secrets. The executor checks only that they are present and never prints their values"
    env_vars:
      - name: MYFEEDER_PG_PASSWORD
        source: "The user's shell. The repo .envrc (direnv) sources $HOME/.config/secrets.env"
      - name: MYFEEDER_ANTHROPIC_API_KEY
        source: "The user's shell. The repo .envrc (direnv) sources $HOME/.config/secrets.env"
      - name: MYFEEDER_RAINDROP_API_TOKEN
        source: "The user's shell, where the repo .envrc exports it. Required here so the deploy keeps Raindrop enabled exactly as before, because deploy.sh would otherwise blank it"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A read-only preflight proves every hop before approval is requested. Origin is reachable. main is merged, clean and unpushed. axion reads 0.1.24-SNAPSHOT. The Docker daemon is up and the registry /v2/ answers. The kube context and Helm release exist, prod reports 0.1.23, and all three deploy secrets are present in the shell"
    - "Per D-05, nothing outward-facing happens until the user answers `approve` at the blocking-human checkpoint. `git push origin main`, the v0.1.24 tag, the image push and ./deploy.sh all come after it"
    - "Per D-05 and the root CLAUDE.md pipeline, the release runs from /Volumes/data2/scottb/dev/bartram/myfeeder in this order: push main, ./gradlew release, ./gradlew clean bootJar, VERSION from currentVersion, docker build --provenance=false, docker push, ./deploy.sh $VERSION, rollout status, logs"
    - "VERSION is exactly 0.1.24, with no -SNAPSHOT, before any image is built. Tag v0.1.24 exists on origin and registry.bartram.org/bartram/myfeeder:0.1.24 exists in the registry (UPG-03)"
    - "The k3s deployment rolls out 0.1.24. `kubectl -n myfeeder rollout status deploy/myfeeder` succeeds, the deployment image ends with :0.1.24, and http://192.168.44.204/api/version reports 0.1.24 (UPG-03)"
    - "Startup is clean. The pod log contains the MyfeederApplication started line, no failed-to-start banner and no ERROR-level line (UPG-03)"
    - "The new frontend bundle is live. The assets/index-*.js name served at http://192.168.44.204/ differs from the pre-deploy name"
    - "Feeds poll as before. At least 20 minutes after rollout, every feed that had errorCount 0 before the deploy still has errorCount 0 and a lastSuccessfulPollAt later than the rollout time, and the pod log has 'Polled feed' lines (UPG-03)"
    - "If the rollout or the startup checks fail, prod goes back to 0.1.23 with ./deploy.sh 0.1.23 (the image is still in the registry and there is no schema change) and /api/version reports 0.1.23 again. The v0.1.24 tag stays, and the fix goes forward"
  artifacts: []
  key_links:
    - from: "main (merged in 01-03)"
      to: "tag v0.1.24 on origin"
      via: "./gradlew release (axion createRelease plus the gitPushRelease Exec task: git push --follow-tags origin HEAD)"
      pattern: "gitPushRelease"
    - from: "build/libs/*.jar (clean bootJar after release)"
      to: "registry.bartram.org/bartram/myfeeder:0.1.24"
      via: "Dockerfile COPY build/libs/*.jar, then docker build --provenance=false and docker push"
      pattern: "COPY build/libs/\\*.jar"
    - from: "registry image 0.1.24"
      to: "deploy/myfeeder in namespace myfeeder"
      via: "./deploy.sh 0.1.24 (helm upgrade --install --set app.image.tag)"
      pattern: "app.image.tag"
  prohibitions:
    - statement: "MUST NOT push main, push a tag, push an image or run deploy.sh before the user's explicit `approve` at the blocking-human checkpoint (D-05)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT force-push, bypass git hooks with --no-verify, pass -Prelease.disableChecks, or delete or move a pushed release tag. Recovery is rollback with ./deploy.sh 0.1.23 or a fix-forward release"
      status: resolved
      verification: judgment
    - statement: "MUST NOT modify production Postgres data or schema. The only permitted production data action is flushing the cache-only Redis, and only on observed cache deserialization errors"
      status: resolved
      verification: judgment
---

<objective>
With the user's explicit approval (D-05), publish and deploy the upgraded app:
- push `main`
- cut release `v0.1.24` with axion
- build and push the Dockerfile image
- deploy it to k3s

Then show that it starts cleanly and that feeds keep polling as before (UPG-03).

Purpose: The phase goal is "myfeeder runs in production on the current patch/GA dependency line and behaves exactly as before". This is the only plan with outward-facing, hard-to-reverse steps, so it leads with a read-only preflight tracer and gates everything published behind one blocking-human decision.

Output:
- `main` and tag `v0.1.24` on origin
- image `registry.bartram.org/bartram/myfeeder:0.1.24`
- a new Helm revision in namespace `myfeeder`
- before and after evidence under `$HOME/.cache/myfeeder-phase01/`
</objective>

<execution_context>
@/Users/scottb/orca/workspaces/myfeeder/main/.claude/gsd-core/workflows/execute-plan.md
@/Users/scottb/orca/workspaces/myfeeder/main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-upgrade/01-CONTEXT.md
@.planning/phases/01-dependency-upgrade/01-RESEARCH.md
@.planning/phases/01-dependency-upgrade/01-03-SUMMARY.md
@deploy.sh
@Dockerfile

The 01-03 SUMMARY is referenced because this plan publishes the merge commit it produced, and its identity/stash notes affect the main worktree state.

Environment facts (verified at planning time):
- Release worktree: `/Volumes/data2/scottb/dev/bartram/myfeeder` (branch `main`). Origin is `git@github.com:sbartram/myfeeder.git` and the repo is PUBLIC. The latest tag is `v0.1.23`. A `.pre-commit-config.yaml` runs trufflehog on pre-commit and pre-push. The git-lfs pre-push hook gotcha from CLAUDE.md was not present in the hooks at planning time.
- `deploy.sh` runs under `set -euo pipefail`. It reads `MYFEEDER_PG_PASSWORD` and `MYFEEDER_ANTHROPIC_API_KEY`, which must be set, and `MYFEEDER_RAINDROP_API_TOKEN`, which falls back to empty and would disable Raindrop. It then runs `helm upgrade --install myfeeder helm/myfeeder -n myfeeder --set app.image.tag=$VERSION ...`. With no argument it computes the next snapshot version, which is why the version is always passed explicitly.
- Production: kube context `k3s-ansible`, namespace `myfeeder`, deployment `myfeeder` (image 0.1.23, Helm revision 16), Redis deployment `myfeeder-redis` (cache only), and Postgres external at pg.bartram.org. The app is served at http://192.168.44.204. At research time there were 46 feeds, 6 of them with errorCount > 0. Baseline logs had about 200 per-feed `Failed to poll feed` WARN lines over 2 days and zero ERROR lines.
- `FeedPollingScheduler` registers every feed at `ApplicationReadyEvent` with `scheduleAtFixedRate`, so each feed polls right after startup and then at its interval.
- The startupProbe allows up to 5 minutes, and a RollingUpdate keeps the 0.1.23 pod serving until the new pod is ready.
- Rollback: `./deploy.sh 0.1.23`. The image is in the registry and this phase adds no Flyway migration.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Release-path dry run, where every hop from main to the running pod answers read-only, and the production baseline is on record</name>
  <files>none (read-only preflight; evidence goes to $HOME/.cache/myfeeder-phase01/preflight.txt)</files>
  <read_first>
    - deploy.sh (secret variables and the helm invocation)
    - Dockerfile (builds from build/libs/*.jar)
    - build.gradle.kts (lines 75-88: release/pushRelease/gitPushRelease)
    - .planning/phases/01-dependency-upgrade/01-03-SUMMARY.md (merge SHA, .serena handling)
    - CLAUDE.md ("Cut a release (full pipeline)" and the Gotchas about release push, git-lfs, SNAPSHOT tags and the stale frontend)
  </read_first>
  <action>
Read-only. Do not push, tag, build an image, deploy or change any file in either worktree. Write each result to `$HOME/.cache/myfeeder-phase01/preflight.txt` as `hop: result`, and never include a secret value. MAIN means `/Volumes/data2/scottb/dev/bartram/myfeeder`.

1. Git:
   - `git -C MAIN ls-remote --exit-code origin refs/heads/main` reaches origin over SSH.
   - `git -C MAIN rev-parse --verify --quiet main^2` shows main's tip is the 01-03 merge.
   - `git -C MAIN status --porcelain` is empty.
   - main is not yet on origin.

2. Version: `(cd MAIN && ./gradlew currentVersion -q)` reads `Project version: 0.1.24-SNAPSHOT`.

3. Image path:
   - `docker info` succeeds.
   - `curl -s -o /dev/null -w '%{http_code}' https://registry.bartram.org/v2/` answers 200 or 401. On 401, confirm that `~/.docker/config.json` has an entry for registry.bartram.org under `auths` or `credHelpers`.

4. Cluster:
   - `kubectl config current-context` is `k3s-ansible`.
   - `kubectl -n myfeeder get deploy myfeeder -o jsonpath='{.spec.template.spec.containers[0].image}'` ends with `:0.1.23`.
   - `helm -n myfeeder status myfeeder` shows `STATUS: deployed`.

5. Secrets, checking presence only: for each of MYFEEDER_PG_PASSWORD, MYFEEDER_ANTHROPIC_API_KEY and MYFEEDER_RAINDROP_API_TOKEN, record `set` or `UNSET` using a `test -n` style check. Never echo, log or `set -x` a value.

6. Production app: `curl -sf http://192.168.44.204/api/version` reports 0.1.23. Record the `assets/index-*.js` bundle name currently served at `/`.

If any hop fails, stop here and report it. The approval checkpoint must only be presented for a pipeline that can run to completion.
  </action>
  <verify>
    <automated>git -C /Volumes/data2/scottb/dev/bartram/myfeeder ls-remote --exit-code origin refs/heads/main > /dev/null && git -C /Volumes/data2/scottb/dev/bartram/myfeeder rev-parse --verify --quiet main^2 > /dev/null && test -z "$(git -C /Volumes/data2/scottb/dev/bartram/myfeeder status --porcelain)" && ! git -C /Volumes/data2/scottb/dev/bartram/myfeeder merge-base --is-ancestor main origin/main</automated>
    <fails_when>non-zero exit: origin is unreachable, main's tip is not the 01-03 merge, the main worktree is dirty, or main is already on origin</fails_when>
    <automated>docker info > /dev/null && kubectl -n myfeeder get deploy myfeeder > /dev/null && helm -n myfeeder status myfeeder > /dev/null && curl -sf http://192.168.44.204/api/version > "$HOME/.cache/myfeeder-phase01/version-before.json" && grep -q '"version":"0.1.23"' "$HOME/.cache/myfeeder-phase01/version-before.json"</automated>
    <fails_when>non-zero exit: the Docker daemon is down, the kube API or Helm release is unreachable, or prod is not currently serving 0.1.23</fails_when>
    <automated>code=$(curl -s -o /dev/null -w '%{http_code}' https://registry.bartram.org/v2/) && { test "$code" = 200 || test "$code" = 401; }</automated>
    <fails_when>non-zero exit: the registry did not answer /v2/ with 200 or 401</fails_when>
    <automated>test -n "${MYFEEDER_PG_PASSWORD:-}" && test -n "${MYFEEDER_ANTHROPIC_API_KEY:-}" && test -n "${MYFEEDER_RAINDROP_API_TOKEN:-}"</automated>
    <fails_when>non-zero exit: at least one deploy secret is absent from the executor's shell</fails_when>
  </verify>
  <acceptance_criteria>
    - `$HOME/.cache/myfeeder-phase01/preflight.txt` has one line per hop (git origin, main merge, clean tree, unpushed, axion version, docker, registry, kube context, deployment image, helm status, the three secrets as set/UNSET, prod version, current bundle name), and every line is a pass.
    - `preflight.txt` contains no secret value. Only the words `set` or `UNSET` appear next to the secret names.
    - `version-before.json` contains `"version":"0.1.23"`.
  </acceptance_criteria>
  <done>Every hop of the release path has been exercised read-only and answers, and the production starting point (0.1.23, current bundle) is recorded. Nothing has been published.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve publishing release 0.1.24 (push main, tag, image, production deploy; D-05)</name>
  <read_first>
    - $HOME/.cache/myfeeder-phase01/preflight.txt (Task 1)
    - .planning/phases/01-dependency-upgrade/01-03-SUMMARY.md (what was verified and merged)
  </read_first>
  <decision>Publish release 0.1.24? This covers `git push origin main`, `./gradlew release` (which creates and pushes tag v0.1.24), pushing image `registry.bartram.org/bartram/myfeeder:0.1.24`, and `./deploy.sh 0.1.24` to namespace `myfeeder` on k3s.</decision>
  <context>
Everything up to here is local and reversible. The merge on the local `main` is unpushed. The preflight in `$HOME/.cache/myfeeder-phase01/preflight.txt` shows every hop answering.

After approval, `main` and tag `v0.1.24` become public on GitHub. A published tag or commit cannot be quietly withdrawn, so undo means a revert or a fix-forward 0.1.25. The image tag is pushed and production is upgraded. The running app can be rolled back with `./deploy.sh 0.1.23`, since the image is in the registry and there is no schema change.

Per CLAUDE.md the user's standing rule is "push only when I ask", and D-05 makes this approval an explicit pause. That is why this checkpoint is gate="blocking-human" and auto-mode never answers it.
  </context>
  <options>
    <option id="approve">
      <name>Approve: run the full "Cut a release" pipeline now</name>
      <pros>Completes UPG-03. Prod moves to the verified 0.1.24 build with the patched Jackson, Spring Cloud Commons and DOMPurify, and the executor handles the post-deploy verification and any rollback.</pros>
      <cons>It publishes to a public repo and upgrades production. Tags and pushed history are permanent.</cons>
    </option>
    <option id="hold">
      <name>Hold: publish nothing now</name>
      <pros>Nothing leaves the workstation. The local `main` keeps the unpushed merge, so it can be pushed later or reset.</pros>
      <cons>UPG-03 and the phase stay incomplete. The local `main` is 1+ commits ahead of origin until it is resolved.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The SUMMARY records the user's answer verbatim. If the answer is `hold`, Task 3 performs no command and the plan ends with UPG-03 open.
  </acceptance_criteria>
  <resume-signal>Select: approve or hold</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Push, release, build, ship and verify 0.1.24 on k3s, then prove feeds poll as before</name>
  <reversibility rating="one-way">Pushes main and tag v0.1.24 to the public GitHub origin and image 0.1.24 to the registry. Published history and tags cannot be quietly withdrawn, so undo is a revert or fix-forward (0.1.25). Production rollback is ./deploy.sh 0.1.23</reversibility>
  <precondition>MYFEEDER_PG_PASSWORD, MYFEEDER_ANTHROPIC_API_KEY and MYFEEDER_RAINDROP_API_TOKEN are non-empty in the executor shell (checked with test -n; values are never printed), and the user answered "approve" at Task 2</precondition>
  <files>none (operational: git push, axion tag, registry image, Helm release; evidence under $HOME/.cache/myfeeder-phase01/)</files>
  <read_first>
    - CLAUDE.md ("Cut a release (full pipeline)"; the Gotchas on `./gradlew release` push, git-lfs pre-push, Gradle terminal escapes, the stale frontend and startupProbe)
    - deploy.sh
    - $HOME/.cache/myfeeder-phase01/preflight.txt
  </read_first>
  <action>
Run everything from `/Volumes/data2/scottb/dev/bartram/myfeeder` (D-05), in exactly this order. Set `C="$HOME/.cache/myfeeder-phase01"`. Never print a secret value and never use `set -x`.

1. Run `git push origin main`. The trufflehog pre-push hook runs. If a hook blocks the push, stop and report; never use `--no-verify`. The one exception is the documented git-lfs "Unable to verify locks" failure (this repo has no LFS content): apply the CLAUDE.md fix `git config lfs.https://github.com/sbartram/myfeeder.git/info/lfs.locksverify false`, then retry once.

2. Run `./gradlew release`. axion's verifyRelease needs a clean tree and main not ahead of origin. It creates `v0.1.24`, and the `gitPushRelease` Exec task pushes it with `--follow-tags`. Never pass `-Prelease.disableChecks`. If it fails, stop and report.

3. Run `./gradlew clean bootJar`. It runs after the release, so build-info carries 0.1.24, and `clean` forces the refreshed SPA bundle into the jar.

4. Run `VERSION=$(./gradlew currentVersion -q | grep 'Project version' | awk '{print $NF}')`. Require `[ "$VERSION" = "0.1.24" ]`, and require that `build/libs/` holds exactly one jar. Otherwise stop: never build or deploy a SNAPSHOT.

5. Run `docker build --provenance=false -t "registry.bartram.org/bartram/myfeeder:$VERSION" .`, then `docker push "registry.bartram.org/bartram/myfeeder:$VERSION"`. Never use bootBuildImage (CLAUDE.md: the Docker 29 containerd diffID bug).

6. Capture the baseline immediately before the deploy:
   - `curl -sf http://192.168.44.204/api/feeds > "$C/feeds-before.json"`
   - save the `assets/index-*.js` name served at `/` into `"$C/bundle-before.txt"`
   - `date -u +%Y-%m-%dT%H:%M:%SZ > "$C/deploy-time.txt"`

7. Run `./deploy.sh "$VERSION"`, always with the explicit version. Then run `kubectl -n myfeeder rollout status deploy/myfeeder --timeout=6m`. On success, write `date -u +%Y-%m-%dT%H:%M:%SZ > "$C/rollout-time.txt"`.

8. Collect startup evidence:
   - `kubectl -n myfeeder logs deploy/myfeeder --since=15m > "$C/startup.log"`
   - `curl -sf http://192.168.44.204/api/version > "$C/version-after.json"`
   - save the served bundle name into `"$C/bundle-after.txt"`

   If `startup.log` shows Redis cache deserialization failures, which research A3 flags as a possibility after the Jackson 3.0 to 3.1 bump, flush the cache-only Redis: `kubectl -n myfeeder exec deploy/myfeeder-redis -- redis-cli FLUSHALL`. Then re-check. Never touch Postgres.

9. Failure path. Trigger it if any of these hold:
   - the rollout does not complete
   - /api/version does not report 0.1.24
   - the startup log has the Spring Boot failed-to-start banner or ERROR-level lines that a cache flush does not clear

   Then run `./deploy.sh 0.1.23` and `kubectl -n myfeeder rollout status deploy/myfeeder --timeout=6m`, confirm /api/version reports 0.1.23, then stop and report. Leave the v0.1.24 tag and image in place and fix forward. Never force-push or delete a tag.

10. Soak: wait until at least 20 minutes have passed since `rollout-time.txt`. Use the runtime's backgrounded or condition-based wait, not a busy loop, and poll the API at most once a minute. Then:
    - `curl -sf http://192.168.44.204/api/feeds > "$C/feeds-after.json"`
    - `kubectl -n myfeeder logs deploy/myfeeder --since=25m > "$C/soak.log"`
    - `curl -sf 'http://192.168.44.204/api/articles?limit=50' > "$C/articles-after.json"`

    Record the newest `fetchedAt` and how many items were fetched after the rollout. Zero new items is not a failure by itself, since publishers may not have posted; the human check confirms the reader.

    If a feed that had errorCount 0 before now has errors:
    - read its `lastError` in `feeds-after.json` and its WARN line in `soak.log`
    - re-check after one more 15-minute poll interval
    - if it persists with an error class that is new since 0.1.23 (for example a transport, TLS, redirect or timeout error), report it as a probable upgrade regression, with `./deploy.sh 0.1.23` as the recommended rollback

    Do not roll back automatically at this stage; the user decides. A 30s timeout on a very slow feed is the D-02 intended behavior change, so call it out explicitly if it happens.
  </action>
  <verify>
    <automated>git -C /Volumes/data2/scottb/dev/bartram/myfeeder ls-remote --exit-code --tags origin refs/tags/v0.1.24 > /dev/null && git -C /Volumes/data2/scottb/dev/bartram/myfeeder merge-base --is-ancestor main origin/main && docker manifest inspect registry.bartram.org/bartram/myfeeder:0.1.24 > /dev/null</automated>
    <fails_when>non-zero exit: tag v0.1.24 is not on origin, main was not pushed, or the 0.1.24 image is missing from the registry</fails_when>
    <automated>kubectl -n myfeeder rollout status deploy/myfeeder --timeout=60s && kubectl -n myfeeder get deploy myfeeder -o jsonpath='{.spec.template.spec.containers[0].image}' > "$HOME/.cache/myfeeder-phase01/deployed-image.txt" && grep -q ':0.1.24$' "$HOME/.cache/myfeeder-phase01/deployed-image.txt" && curl -sf http://192.168.44.204/api/version > "$HOME/.cache/myfeeder-phase01/version-after.json" && grep -q '"version":"0.1.24"' "$HOME/.cache/myfeeder-phase01/version-after.json"</automated>
    <fails_when>non-zero exit: the rollout is incomplete, the deployment image is not :0.1.24, or /api/version does not report 0.1.24</fails_when>
    <automated>grep -q 'Started MyfeederApplication' "$HOME/.cache/myfeeder-phase01/startup.log" && ! grep -q 'APPLICATION FAILED TO START' "$HOME/.cache/myfeeder-phase01/startup.log" && ! grep -q ' ERROR ' "$HOME/.cache/myfeeder-phase01/startup.log" && test -s "$HOME/.cache/myfeeder-phase01/bundle-after.txt" && ! cmp -s "$HOME/.cache/myfeeder-phase01/bundle-before.txt" "$HOME/.cache/myfeeder-phase01/bundle-after.txt"</automated>
    <fails_when>non-zero exit: no started line in the pod log, the failed-to-start banner or an ERROR-level line is present, or the served SPA bundle is unchanged (a stale frontend shipped)</fails_when>
    <automated>node -e 'const fs=require("fs"),d=process.env.HOME+"/.cache/myfeeder-phase01/";const ts=v=>typeof v==="number"?(v<1e12?v*1000:v):Date.parse(v);const t=Date.parse(fs.readFileSync(d+"rollout-time.txt","utf8").trim());const b=JSON.parse(fs.readFileSync(d+"feeds-before.json","utf8"));const a=new Map(JSON.parse(fs.readFileSync(d+"feeds-after.json","utf8")).map(f=>[f.id,f]));const healthy=b.filter(f=>f.errorCount===0);const bad=healthy.filter(f=>{const x=a.get(f.id);return !x||x.errorCount>0||!x.lastSuccessfulPollAt||!(ts(x.lastSuccessfulPollAt)>t)});if(Number.isNaN(t)||Date.now()-t<20*60*1000||healthy.length===0||bad.length){console.log(JSON.stringify({soakMinutes:Math.floor((Date.now()-t)/60000),bad:bad.map(f=>[f.id,f.title])}));process.exit(1)}console.log("healthy feeds re-polled cleanly: "+healthy.length+"/"+b.length)'</automated>
    <fails_when>non-zero exit: fewer than 20 minutes have passed since rollout, the rollout time is unreadable, or some feed that was healthy before the deploy now has errors or has no successful poll since the rollout (the ids and titles are printed)</fails_when>
    <automated>grep -q 'Polled feed' "$HOME/.cache/myfeeder-phase01/soak.log"</automated>
    <fails_when>non-zero exit: the pod logged no successful feed poll during the soak window</fails_when>
    <human-check>
      <test>Open http://192.168.44.204 in a browser. Load the feed tree, open a feed with recent items and open an article, including Reader View once. Compare the newest articles' times with the deploy time in $HOME/.cache/myfeeder-phase01/deploy-time.txt (the fetchedAt values are in articles-after.json).</test>
      <expected>The three-panel reader loads with your usual theme. Articles fetched after the deploy appear in the list and open in the reading pane, and reader view works. Nothing looks or behaves differently from 0.1.23.</expected>
      <why_human>ROADMAP Phase 1 success criterion 3 needs visual confirmation that "new articles appear in the reader as before". Whether publishers post during the soak window cannot be forced, and UI regressions from the React 19.3 / TanStack Query refresh cannot be grepped.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --tags origin` shows `refs/tags/v0.1.24`, `main` is on origin, and `docker manifest inspect registry.bartram.org/bartram/myfeeder:0.1.24` succeeds.
    - `VERSION` was exactly `0.1.24` before `docker build` (recorded in the SUMMARY), and `build/libs/` held one jar.
    - The deployment image ends with `:0.1.24`, the rollout is complete, and `version-after.json` contains `"version":"0.1.24"`.
    - `startup.log` contains `Started MyfeederApplication` and has no failed-to-start banner and no ERROR-level line. `bundle-after.txt` differs from `bundle-before.txt`.
    - At least 20 minutes after rollout, every feed with `errorCount` 0 in `feeds-before.json` has `errorCount` 0 and a `lastSuccessfulPollAt` after `rollout-time.txt` in `feeds-after.json`. `soak.log` contains `Polled feed`. The SUMMARY records the healthy/total counts and the number of articles fetched after the rollout.
    - If the failure path ran, the SUMMARY records it, and /api/version reported 0.1.23 afterwards.
    - No secret value appears in any evidence file, log excerpt or the SUMMARY.
  </acceptance_criteria>
  <done>0.1.24 is published and running on k3s with a clean startup, the new frontend is live, and feeds that were healthy before keep polling successfully. The only remaining step is the batched human check of the reader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workstation → GitHub origin (public repo) | Commits and tags become permanently public |
| Workstation → registry.bartram.org | The pushed image is what production runs |
| Workstation → k3s API (Helm) | Deploy secrets cross here as `--set secrets.*` values |
| Internet feed servers → production app | Untrusted feed content and network behavior, now on Reactor Netty 1.3.7 with 5s/30s timeouts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Information Disclosure | Deploy secrets (shell env → deploy.sh → helm --set) | high | mitigate | Presence checks only (`test -n`). `preflight.txt` records only `set`/`UNSET`. No `set -x`, no echo. deploy.sh is unchanged. The trufflehog pre-push hook runs, because `--no-verify` is forbidden, and nothing from the evidence directory is committed (the repo is public). |
| T-01-13 | Tampering | Published artifact (tag, jar, image) | high | mitigate | Blocking-human approval (Task 2) comes before any publish. The order is `release` before `clean bootJar`. The build stops unless VERSION is exactly 0.1.24 with a single jar, and the image is built from the Dockerfile with `--provenance=false`. The merge tree was verified in 01-03 against the smoked SHA. |
| T-01-14 | Denial of Service | Production rollout (deploy/myfeeder) | high | mitigate | RollingUpdate keeps 0.1.23 serving until the new pod passes its startupProbe. `rollout status` has a timeout. The failure path runs `./deploy.sh 0.1.23` automatically and confirms /api/version is 0.1.23. There is no schema change, so rollback is safe. |
| T-01-15 | Tampering | Redis cache values across the Jackson 3.0 → 3.1 bump | low | mitigate | FLUSHALL on the cache-only `myfeeder-redis`, and only on observed deserialization errors. Postgres is never touched. |
| T-01-16 | Repudiation | Release provenance | low | mitigate | The axion tag `v0.1.24` sits on the `--no-ff` merge commit, `/api/version` reports the build-info version, and the SUMMARY records the commit, tag and image. |
</threat_model>

<verification>
- The Task 1 tracer checks every hop read-only (origin, merge, clean tree, unpushed, axion 0.1.24-SNAPSHOT, docker, registry, kube, helm, secrets present, prod on 0.1.23).
- Task 2: the blocking-human `approve` is recorded, or the plan stops on `hold`.
- Task 3: the tag and image are published, the rollout is complete on :0.1.24, /api/version reports 0.1.24, the startup is clean, the bundle changed, the soak of at least 20 minutes shows every previously healthy feed re-polled cleanly and `Polled feed` log lines, and the human check of the reader is queued for end-of-phase UAT.
</verification>

<success_criteria>
- UPG-03: the upgraded app is released as v0.1.24 and deployed to k3s, starts cleanly, and feeds keep polling as before.
- D-05: the release ran from the `main` worktree after the `--no-ff` merge, and the push happened only after explicit user approval, following the documented pipeline order.
</success_criteria>

## Artifacts this phase produces

| Artifact | Produced by | Notes |
|----------|-------------|-------|
| Backend upgrade files and root CLAUDE.md lines | 01-01 | |
| Frontend manifest and lockfile | 01-02 | |
| STACK.md and the local `--no-ff` merge on `main` | 01-03 | |
| `main` pushed to origin | **01-04 (this plan)** | After approval |
| Git tag `v0.1.24` on origin | **01-04 (this plan)** | Created by `./gradlew release` |
| Jar `build/libs/myfeeder-0.1.24.jar` (main worktree, gitignored) | **01-04 (this plan)** | Built by `clean bootJar` after the release |
| Image `registry.bartram.org/bartram/myfeeder:0.1.24` | **01-04 (this plan)** | Dockerfile build, `--provenance=false` |
| Helm release `myfeeder` revision (namespace `myfeeder`) on image tag 0.1.24 | **01-04 (this plan)** | Via `./deploy.sh 0.1.24` |

New symbols created by this plan (for source-grounding / drift exclusion): none in source code. The evidence files are `preflight.txt`, `version-before.json`, `feeds-before.json`, `bundle-before.txt`, `deploy-time.txt`, `rollout-time.txt`, `startup.log`, `version-after.json`, `bundle-after.txt`, `deployed-image.txt`, `feeds-after.json`, `soak.log` and `articles-after.json`, all under `$HOME/.cache/myfeeder-phase01/`.

## Flagged Assumptions (edge probe: UPG-03, unclassified, left unresolved)

The edge probe could not classify UPG-03. Per the no-silent-drop rule it stays **unresolved**. The planner sees these candidate edges but does not auto-resolve them:
1. **Redis cache compatibility across Jackson 3.0.4 → 3.1.5 (research A3).** This is unverified until production reads old cache entries. The mitigation is a cache-only FLUSHALL, and only on observed errors.
2. **New articles arriving during the soak window** depends on publishers. The automated checks prove polling succeeds; only the human check can confirm that "new articles appear in the reader".
3. **A healthy feed failing during the window for reasons unrelated to the upgrade** (a remote outage or rate limit). This is judged from `lastError` and a second poll, not automatically, and is reported with a rollback recommendation rather than auto-rolled back.
4. **Real-network behavior changes that only production can show**: Reactor Netty 1.3.3 → 1.3.7, and the now-bound 30s read timeout (research A5, the D-02 intended change). The before/after errorCount comparison is the detector.

## Canon-referral breadcrumbs (prohibition probe)

- Secret leakage during deploy (helm `--set`, shell echo) is canon, covered by T-01-12, the trufflehog hooks and /gsd-secure-phase. It is not minted as a prohibition.

<output>
Create `.planning/phases/01-dependency-upgrade/01-04-SUMMARY.md` when done. Include the user's Task 2 answer, the pushed main SHA, the tag, VERSION, the image digest from `docker push`, the Helm revision, the rollout time, the startup check results, the bundle before and after, the healthy/total feed re-poll counts, the articles fetched after the rollout, whether the failure path or the cache flush ran, and any feed whose health changed with its lastError.
</output>
