---
phase: 01-dependency-upgrade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/frontend/package.json
  - src/main/frontend/package-lock.json
autonomous: false
requirements: [UPG-02]
coupling_justified:
  - "01-01: 01-02's `npm run build` writes the gitignored src/main/resources/static/, which 01-01's processResources copies. No backend test reads static/, and 01-01 excludes npmInstall/npmBuild, so both orders give a correct result"

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every frontend dependency is on the latest release within its current major. After install, `npm outdated --json` lists no package whose current version differs from its wanted version (UPG-02)"
    - "No major version is crossed: react-router-dom is still 6.x, typescript is still 5.9.x (`~5.9.3`), and every direct dependency keeps its major (or its minor, for 0.x packages)"
    - "The dependency set is unchanged: package.json has exactly the same dependency and devDependency names as before, and only version ranges move"
    - "`npx tsc -b`, `npm test` and `npm run build` all pass on the updated lockfile (UPG-02)"
    - "Per D-04 the versions come from plain `npm update --save`, with no `--before` cooldown"
    - "Per D-04 and the package-legitimacy gate: no newly resolved package is downloaded or executed, and nothing is committed, until the user approves at a blocking-human checkpoint. The checkpoint shows each changed direct dependency's publish time, with under-48-hour releases flagged, and any new install scripts"
    - "The installed lockfile is byte-identical to the one the user approved (sha256 recorded before the checkpoint and checked after)"
    - "npm audit reports zero high and zero critical advisories after the update"
  artifacts:
    - path: "src/main/frontend/package.json"
      provides: "Version ranges refreshed by npm update --save within current majors"
      contains: "\"react-router-dom\": \"^6."
    - path: "src/main/frontend/package-lock.json"
      provides: "Resolved in-major versions, reviewed at the checkpoint and installed with npm ci"
      contains: "node_modules/react-router-dom"
  key_links:
    - from: "src/main/frontend/package-lock.json"
      to: "Gradle npmInstall/npmBuild (build.gradle.kts)"
      via: "the lockfile is an npmInstall/npmBuild input, so ./gradlew clean bootJar embeds the refreshed bundle"
      pattern: "package-lock.json"
    - from: "D-04 checkpoint approval"
      to: "npm ci install"
      via: "sha256 of the reviewed lockfile checked before npm ci"
      pattern: "npm-approved.sha256"
  prohibitions:
    - statement: "MUST NOT run, install or commit newly resolved package versions before the user approves the version set at the blocking-human checkpoint (D-04)"
      status: resolved
      verification: judgment
---

<objective>
Refresh the React/TypeScript frontend to the latest minor/patch release inside every current major version (UPG-02). react-router stays on v6, and TypeScript stays on 5.9. Per D-04, use plain `npm update --save` and put a human check on the resolved versions before anything is committed.

Purpose: This is the frontend half of the phase. It clears 13 of the 15 current npm audit findings, including DOMPurify and vite advisories. The 2 react-router v6 moderates remain; their fix needs v7, which is deferred. Two of the target versions were published less than 48 hours before research, so the user checks the version set first.

Output: one commit with `src/main/frontend/package.json` and `src/main/frontend/package-lock.json`, plus a candidate table and approval hash in `$HOME/.cache/myfeeder-phase01/`.
</objective>

<execution_context>
@/Users/scottb/orca/workspaces/myfeeder/main/.claude/gsd-core/workflows/execute-plan.md
@/Users/scottb/orca/workspaces/myfeeder/main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-upgrade/01-CONTEXT.md
@.planning/phases/01-dependency-upgrade/01-RESEARCH.md
@src/main/frontend/package.json

Environment facts (verified at planning time):
- npm 11.19.1, Node v26.9.0. The frontend lives in `src/main/frontend/`, which has `package.json`, `package-lock.json`, `tsconfig.json`, `vite.config.ts` and `vitest.config.ts`.
- `main` (02f74a1) and the phase-start `sbartram/main` have identical frontend files, so `main:src/main/frontend/package*.json` is the pre-upgrade baseline for this whole phase until plan 01-03 merges.
- Type-check with `npx tsc -b` from `src/main/frontend/`. Never use `tsc --noEmit`: the root tsconfig has `files: []`, so it passes vacuously.
- Research (scratch run): `npm update --save` bumps 20 packages. Two targets were under 48 hours old at research time: `@tanstack/react-query` 5.103.2 and `typescript-eslint` 8.70.1. `npm audit` goes from 15 findings to 2 moderate react-router v6 advisories. Versions may have moved by execution time, and the table produced in Task 1 is authoritative.
- Research quirk: `npm update --save` installs `@eslint/js` 9.39.5 but leaves its range at `^9.39.4`. Do not hand-edit it. The gate compares installed against wanted through the lockfile, not package.json floors.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Resolve the in-major update set from manifest to registry to lockfile without installing anything</name>
  <files>src/main/frontend/package.json, src/main/frontend/package-lock.json</files>
  <read_first>
    - src/main/frontend/package.json (current ranges: react-router-dom `^6.30.3`, typescript `~5.9.3`, eslint-plugin-react-refresh `^0.5.2`)
    - .planning/phases/01-dependency-upgrade/01-RESEARCH.md (the "Frontend: target versions" table and the "Package Legitimacy Audit" section)
    - .planning/phases/01-dependency-upgrade/01-CONTEXT.md (D-04)
  </read_first>
  <action>
1. Run `mkdir -p "$HOME/.cache/myfeeder-phase01"`. Snapshot the baseline with `git show main:src/main/frontend/package.json > "$HOME/.cache/myfeeder-phase01/package.main.json"` and `git show main:src/main/frontend/package-lock.json > "$HOME/.cache/myfeeder-phase01/package-lock.main.json"`. Confirm that `git diff --quiet main -- src/main/frontend/package.json src/main/frontend/package-lock.json` exits 0, meaning the working files equal the baseline before this task. If it does not, stop and report.

2. Per D-04, resolve with plain `npm update --save`, with no `--before` cooldown. Add only `--package-lock-only`: `npm --prefix src/main/frontend update --save --package-lock-only`. That flag rewrites package.json and package-lock.json without downloading or installing anything. It changes nothing about which versions are selected. It only moves the install until after the blocking-human checkpoint, as the package-legitimacy gate requires.

   Fallback, if npm rejects the flag or leaves package-lock.json unchanged: restore both files with `git checkout -- src/main/frontend/package.json src/main/frontend/package-lock.json`. Build the candidate list read-only from the `wanted` column of `npm --prefix src/main/frontend outdated --json`, and install nothing. Task 3 then runs plain `npm --prefix src/main/frontend update --save` after approval, and must check that the installed versions equal the approved ones. Record which path was used.

3. Write the candidate table to `$HOME/.cache/myfeeder-phase01/npm-candidates.md`. List every direct dependency whose lockfile version changed (`packages["node_modules/<name>"].version`, baseline against new), with these columns:
   - name
   - old version
   - new version
   - publish time in UTC, from `npm view <name> time --json`, keyed by the new version
   - age in hours when the table was made
   - install scripts, from `npm view <name>@<new> scripts --json` (preinstall, install, postinstall keys only)
   - a `<48h` flag

   Add a second section that lists every lockfile entry, direct or transitive, that has `hasInstallScript: true` in the new lockfile but did not in the baseline lockfile at the same path. Add one line with the count of changed transitive entries.

4. Record the reviewed files: `shasum -a 256 src/main/frontend/package.json src/main/frontend/package-lock.json > "$HOME/.cache/myfeeder-phase01/npm-approved.sha256"`.

5. Do NOT run `npm install`, `npm ci`, `tsc`, `vitest` or `vite` in this task. No newly resolved package code may run before the checkpoint. Do not commit.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs"),d=process.env.HOME+"/.cache/myfeeder-phase01/";const op=JSON.parse(fs.readFileSync(d+"package.main.json","utf8")),ol=JSON.parse(fs.readFileSync(d+"package-lock.main.json","utf8"));const np=JSON.parse(fs.readFileSync("src/main/frontend/package.json","utf8")),nl=JSON.parse(fs.readFileSync("src/main/frontend/package-lock.json","utf8"));const names=p=>Object.keys(Object.assign({},p.dependencies,p.devDependencies)).sort();if(names(op).join()!==names(np).join()){console.log("dependency set changed");process.exit(1)}const v=(l,n)=>l.packages["node_modules/"+n].version;const k=s=>{const [a,b]=s.split(".");return a==="0"?a+"."+b:a};let changed=0;const bad=[];for(const n of names(np)){const a=v(ol,n),b=v(nl,n);if(a!==b)changed++;if(k(a)!==k(b))bad.push(n+" "+a+" -> "+b)}const rr=v(nl,"react-router-dom"),ts=v(nl,"typescript");if(!rr.startsWith("6."))bad.push("react-router-dom "+rr);if(!ts.startsWith("5.9."))bad.push("typescript "+ts);if(bad.length||changed===0){console.log(JSON.stringify({changed,bad}));process.exit(1)}console.log("changed direct deps: "+changed)'</automated>
    <fails_when>non-zero exit: the dependency name set changed, a direct dependency crossed a major (or a 0.x minor), react-router-dom left 6.x, typescript left 5.9.x, or nothing changed at all (the update did not resolve)</fails_when>
    <automated>test -s "$HOME/.cache/myfeeder-phase01/npm-candidates.md" && grep -q '48h' "$HOME/.cache/myfeeder-phase01/npm-candidates.md" && shasum -a 256 -c "$HOME/.cache/myfeeder-phase01/npm-approved.sha256"</automated>
    <fails_when>non-zero exit: the candidate table is missing or has no under-48h column, or the recorded hash does not match the resolved files</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/main/frontend/package.json` still has `"react-router-dom": "^6.` and `"typescript": "~5.9.3"`, and has the same dependency and devDependency names as `main:src/main/frontend/package.json`.
    - In the new lockfile, every changed direct dependency keeps its major (or its minor for 0.x), and at least one direct dependency changed.
    - `$HOME/.cache/myfeeder-phase01/npm-candidates.md` has one row per changed direct dependency, with publish time, age in hours, install scripts and a `<48h` flag, plus the new-install-script section and the transitive change count.
    - `$HOME/.cache/myfeeder-phase01/npm-approved.sha256` matches the current package.json and package-lock.json.
    - The SUMMARY states which path ran (`--package-lock-only` or the read-only fallback) and that no install, tsc, vitest or vite command ran in this task.
  </acceptance_criteria>
  <done>The exact in-major version set is resolved into the manifest and lockfile (or listed read-only in the fallback), each changed package's age and install scripts are tabulated, and no new package code has run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the resolved npm version set, including releases under 48 hours old (D-04 and package-legitimacy gate)</name>
  <read_first>
    - $HOME/.cache/myfeeder-phase01/npm-candidates.md (the table produced by Task 1)
    - .planning/phases/01-dependency-upgrade/01-RESEARCH.md ("Package Legitimacy Audit")
  </read_first>
  <what-built>
Task 1 resolved the latest in-major version of every frontend dependency into `src/main/frontend/package.json` and `package-lock.json`, with plain `npm update --save` and no cooldown, per D-04. Nothing has been installed, run or committed. The table at `$HOME/.cache/myfeeder-phase01/npm-candidates.md` lists every changed direct dependency with its publish time, age in hours, install scripts and a `<48h` flag, plus any package that newly carries an install script.

This gate uses gate="blocking-human" on purpose. It is the package-legitimacy check the planner protocol requires before any install, and the D-04 check before committing. Auto-mode and the end-of-phase human-verify default do not bypass it.
  </what-built>
  <how-to-verify>
    1. Open `$HOME/.cache/myfeeder-phase01/npm-candidates.md`.
    2. For each row flagged `<48h`, open https://www.npmjs.com/package/NAME/v/VERSION (at research time these were `@tanstack/react-query` 5.103.2 and `typescript-eslint` 8.70.1). Confirm the publisher and maintainers match earlier releases, and that the project's GitHub releases or changelog show the version.
    3. Confirm the "new install scripts" section is empty, or that each entry listed there is expected.
    4. Confirm react-router-dom is 6.x, typescript is 5.9.x, and no row crosses a major version.
  </how-to-verify>
  <acceptance_criteria>
    - The user's resume signal is recorded verbatim in the SUMMARY.
    - If the user holds packages back, their names are recorded one per line in `$HOME/.cache/myfeeder-phase01/npm-hold.txt`, and the SUMMARY lists them as an explicit, user-approved deviation from UPG-02's "latest minor/patch" criterion.
  </acceptance_criteria>
  <resume-signal>Type "approved" to install and commit exactly these versions, or "hold: name1, name2" to keep those packages at their previous versions (recorded as a deviation), or describe a concern to stop.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install the approved lockfile, prove type-check, tests, bundle build and the in-major gate, then commit</name>
  <files>src/main/frontend/package.json, src/main/frontend/package-lock.json</files>
  <read_first>
    - src/main/frontend/package.json (as resolved in Task 1)
    - $HOME/.cache/myfeeder-phase01/npm-candidates.md and $HOME/.cache/myfeeder-phase01/npm-approved.sha256 (Task 1 outputs)
    - $HOME/.cache/myfeeder-phase01/npm-hold.txt (only if the user held packages back at Task 2)
  </read_first>
  <action>
1. Holds, only if the user answered `hold: ...` at Task 2: for each held package, run `npm --prefix src/main/frontend install NAME@OLD_VERSION --save --package-lock-only`, using the old version from the table. Write the names to `$HOME/.cache/myfeeder-phase01/npm-hold.txt`, one per line, then re-record `npm-approved.sha256`.

2. Drift guard: `shasum -a 256 -c "$HOME/.cache/myfeeder-phase01/npm-approved.sha256"` must pass. The files about to be installed must be exactly the ones the user approved. If it fails, stop and report; do not re-resolve silently.

3. Install exactly the approved lockfile with `npm --prefix src/main/frontend ci`. If Task 1 took the read-only fallback path, run `npm --prefix src/main/frontend update --save` instead. Then confirm that every changed direct dependency's installed lockfile version equals the approved new version in the table. If any differs, stop and report.

4. Prove it:
   - `(cd src/main/frontend && npx tsc -b)`
   - `npm --prefix src/main/frontend test` (vitest run)
   - `npm --prefix src/main/frontend run build` (tsc -b plus vite build into the gitignored `src/main/resources/static/`)

   If any of these fails, fix only within the scope of a dependency refresh (for example a changed type in an updated package) and record the fix. Do not upgrade across a major, and do not edit unrelated application code.

5. Gates, all of which are in the verify block:
   - `npm outdated --json` lists no package with current different from wanted, except held packages
   - react-router-dom is 6.x
   - `npm audit --json` reports zero high and zero critical advisories

   Record the moderate/low counts in the SUMMARY. Research expects the 2 moderate react-router v6 advisories, whose fix is v7 and deferred, so they are accepted. If a high or critical advisory appears whose fix needs a major bump, stop and report instead of crossing a major.

6. Commit exactly `src/main/frontend/package.json` and `src/main/frontend/package-lock.json`, staged by explicit path. Message: `build(01-02): refresh frontend deps within current majors (npm update --save, D-04)`. Never stage `src/main/resources/static/` (it is gitignored) or anything under `.claude/`.
  </action>
  <verify>
    <automated>(cd src/main/frontend && npx tsc -b) && npm --prefix src/main/frontend test</automated>
    <fails_when>non-zero exit, a TypeScript "error TS" line, or a non-zero "failed" count in the vitest summary</fails_when>
    <automated>npm --prefix src/main/frontend run build</automated>
    <fails_when>non-zero exit, or the vite build prints an error instead of the built asset list</fails_when>
    <automated>npm --prefix src/main/frontend outdated --json > "$HOME/.cache/myfeeder-phase01/npm-outdated.json"; node -e 'const fs=require("fs"),d=process.env.HOME+"/.cache/myfeeder-phase01/";const o=JSON.parse(fs.readFileSync(d+"npm-outdated.json","utf8"));const h=fs.existsSync(d+"npm-hold.txt")?fs.readFileSync(d+"npm-hold.txt","utf8").split("\n").map(s=>s.trim()).filter(Boolean):[];const bad=Object.entries(o).filter(([n,x])=>x.current!==x.wanted&&!h.includes(n));if(bad.length){console.log(JSON.stringify(bad));process.exit(1)}console.log("in-major gate clean; holds: "+h.length)'</automated>
    <fails_when>non-zero exit: some package (not held back at the checkpoint) has an installed version older than its wanted in-major version, or the npm outdated output is not valid JSON</fails_when>
    <automated>node -e 'const v=require("./src/main/frontend/package-lock.json").packages["node_modules/react-router-dom"].version;if(!v.startsWith("6.")){console.log(v);process.exit(1)}' && npm --prefix src/main/frontend audit --json > "$HOME/.cache/myfeeder-phase01/npm-audit.json"; node -e 'const a=JSON.parse(require("fs").readFileSync(process.env.HOME+"/.cache/myfeeder-phase01/npm-audit.json","utf8")).metadata.vulnerabilities;console.log(JSON.stringify(a));if(a.high+a.critical>0)process.exit(1)'</automated>
    <fails_when>non-zero exit: react-router-dom is not 6.x, or npm audit reports any high or critical advisory</fails_when>
    <automated>git show --name-only --format= "$(git log -1 --format=%H -- src/main/frontend/package-lock.json)" > "$HOME/.cache/myfeeder-phase01/npm-commit-files.txt" && grep -qx 'src/main/frontend/package-lock.json' "$HOME/.cache/myfeeder-phase01/npm-commit-files.txt" && grep -qx 'src/main/frontend/package.json' "$HOME/.cache/myfeeder-phase01/npm-commit-files.txt" && ! grep -vxE '(src/main/frontend/package(-lock)?\.json)?' "$HOME/.cache/myfeeder-phase01/npm-commit-files.txt"</automated>
    <fails_when>non-zero exit: the commit is missing package.json or package-lock.json, or it contains any other file</fails_when>
  </verify>
  <acceptance_criteria>
    - `shasum -a 256 -c "$HOME/.cache/myfeeder-phase01/npm-approved.sha256"` passed before `npm ci` ran (recorded in the SUMMARY).
    - `npx tsc -b`, `npm test` and `npm run build` exit 0. The SUMMARY records the vitest file and test totals as printed, and the new `assets/index-*.js` bundle name.
    - In `npm outdated --json`, current equals wanted for every package except held ones. react-router-dom's lockfile version starts with `6.`, and typescript's starts with `5.9.`.
    - `npm audit --json` shows high 0 and critical 0. The SUMMARY records the remaining moderate/low counts and names the accepted react-router v6 advisories.
    - The commit contains exactly `src/main/frontend/package.json` and `src/main/frontend/package-lock.json`.
  </acceptance_criteria>
  <done>The approved in-major version set is installed, type-checks, tests green, builds, passes the in-major and audit gates, and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer workstation / build | Third-party package code crosses here. It runs during install scripts, tsc, vitest and vite, and ships in the production bundle |
| Production bundle → user's browser | Feed and extracted HTML is rendered after DOMPurify sanitization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (package.json / package-lock.json) | high | mitigate | Package-legitimacy gate: Task 1 resolves with `--package-lock-only`, so nothing is downloaded or executed. It tabulates publish ages, under-48h flags and new install scripts. The Task 2 blocking-human checkpoint approves the set. Task 3 checks the approved sha256 and then runs `npm ci` from that exact lockfile. The lockfile is committed. |
| T-01-05 | Tampering | DOMPurify 3.4.1 → 3.4.x (ReadingPane sanitization) | medium | mitigate | The sanitize config (`FORBID_TAGS/FORBID_ATTR: ['style']`) is not touched. The existing ReadingPane tests run in `npm test`. The upgrade itself fixes the published DOMPurify ≤3.4.12 advisories. |
| T-01-06 | Tampering | react-router-dom 6.x (GHSA-wrjc-x8rr-h8h6 open redirect, GHSA-337j-9hxr-rhxg SSR hydration) | medium | accept | The fix needs v7, and CONTEXT defers that major bump. The SPA has no SSR and its navigation targets are app-internal, so the open-redirect vector is not reachable from feed content. Record it as accepted in the SUMMARY. |
| T-01-07 | Information Disclosure | vite dev-server advisories (`server.fs.deny` bypass, launch-editor) | low | accept | These are dev-server only and fixed by vite 8.3.x. The production artifact is a static bundle served by Spring. |
</threat_model>

<verification>
- The Task 1 tracer resolves the in-major set into the manifest and lockfile without installing, with no major crossed and react-router-dom and typescript pinned to 6.x and 5.9.x.
- The Task 2 blocking-human approval is recorded, with holds listed as explicit deviations.
- Task 3: the sha256 drift guard passes, then `npm ci`, `npx tsc -b`, `npm test`, `npm run build`, the npm outdated gate, the react-router 6.x check and the audit gate (high+critical = 0) all pass. One commit contains exactly the two manifest files.
</verification>

<success_criteria>
- UPG-02: frontend dependencies are on the latest minor/patch in each current major, with no major bumps and react-router still on v6, and `npm test` and `npx tsc -b` pass.
- D-04: the versions come from plain `npm update --save`, and the user approved the version set, including releases under 48 hours old, before commit and before install.
</success_criteria>

## Artifacts this phase produces

| Artifact | Produced by | Notes |
|----------|-------------|-------|
| `build.gradle.kts`, `HttpClientConfigurationTest.java`, `application.yaml`, root `CLAUDE.md` lines | 01-01 | Backend upgrade, D-01/D-02 |
| `src/main/frontend/package.json` (ranges only) | **01-02 (this plan)** | Dependency names unchanged |
| `src/main/frontend/package-lock.json` | **01-02 (this plan)** | Approved and hash-pinned |
| `.planning/codebase/STACK.md` | 01-03 | Records the resolved frontend versions from this plan's lockfile |
| Merge commit on `main`, tag `v0.1.24`, image `:0.1.24`, Helm revision | 01-03 / 01-04 | D-05 |

New symbols created by this plan (for source-grounding / drift exclusion): none in source code. The evidence files are `package.main.json`, `package-lock.main.json`, `npm-candidates.md`, `npm-approved.sha256`, `npm-hold.txt` (only with holds), `npm-outdated.json`, `npm-audit.json` and `npm-commit-files.txt`, all under `$HOME/.cache/myfeeder-phase01/`.

## Flagged Assumptions (edge probe: UPG-02, unclassified, left unresolved)

The edge probe could not classify UPG-02. Per the no-silent-drop rule it stays **unresolved**. The planner sees these candidate edges but does not auto-resolve them:
1. **A release under 48 hours old at execution time.** A human judges this at the Task 2 checkpoint, not an automated rule. The approval covers the exact lockfile hash.
2. **npm leaves a package.json range floor below the installed version** (the `@eslint/js` quirk). The in-major gate reads the installed and wanted versions from the lockfile, so range floors are never compared and are not hand-edited.
3. **A transitive package newly gains an install script.** It is listed in the candidate table for human review. No automated rule rejects it.

## Canon-referral breadcrumbs (prohibition probe)

- Supply-chain compromise of npm packages is canon (OWASP vulnerable-components), covered by T-01-SC and /gsd-secure-phase. It is not minted as a separate prohibition.
- Weakening DOMPurify/XSS sanitization is canon injection, covered by T-01-05 and /gsd-secure-phase. It is not minted.

<output>
Create `.planning/phases/01-dependency-upgrade/01-02-SUMMARY.md` when done. Include the candidate table (or a pointer to it plus its row count), the user's verbatim resume signal, any holds, the drift-guard result, the tsc/test/build results, the npm audit counts and the commit SHA.
</output>
