---
phase: 01-dependency-upgrade
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - .planning/codebase/STACK.md
autonomous: false
requirements: [UPG-01, UPG-02, UPG-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The combined tree (01-01 plus 01-02) passes `./gradlew clean build` with Docker. That covers npmInstall, npmBuild and every test class. There are at least 31 result files, including HttpClientConfigurationTest and the Testcontainers classes, all with zero failures and errors (UPG-01, UPG-02)"
    - "`./gradlew clean build` does not rewrite src/main/frontend/package.json or package-lock.json, so npmInstall is consistent with the committed lockfile"
    - "Locally (bootTestRun with Testcontainers Postgres and Redis), the upgraded app starts. It serves the SPA at /, answers /api/version, subscribes a real public feed through the Reactor Netty transport with bound timeouts, and returns that feed's articles, with no log line at ERROR level"
    - "Per D-05, the user settles the main worktree's uncommitted .serena/project.yml at a blocking-human checkpoint before the merge, and it is never silently discarded. The `restore` option is applied only after re-confirming the file is byte-identical to the content the merge brings in"
    - "Per D-05, sbartram/main is merged into main with --no-ff in /Volumes/data2/scottb/dev/bartram/myfeeder. The merge commit's tree equals its second parent, and its code paths (src, build.gradle.kts, gradle, Dockerfile, helm, deploy.sh) are identical to the commit Task 1 verified"
    - "Per D-05 this plan pushes nothing. origin/main is an ancestor of main, and main is not yet on origin, because the push waits for the 01-04 approval"
    - "After the merge, axion computes 0.1.24-SNAPSHOT on main, so `./gradlew release` in 01-04 cuts 0.1.24 (D-05)"
    - "STACK.md records Boot 4.0.8, Spring AI 2.0.1, Spring Cloud 2025.1.3, the spring.http.clients.* keys, the Reactor Netty transport and the installed frontend versions. No other version is claimed to have moved (D-03)"
  artifacts:
    - path: ".planning/codebase/STACK.md"
      provides: "Codebase stack map updated to the resolved backend and frontend versions"
      contains: "Spring Boot 4.0.8"
  key_links:
    - from: "sbartram/main (verified in Task 1)"
      to: "main in /Volumes/data2/scottb/dev/bartram/myfeeder"
      via: "git merge --no-ff; tree equality with main^2 and a code-path diff against the Task 1 verified SHA"
      pattern: "merge --no-ff sbartram/main"
    - from: "main (merged, unpushed)"
      to: "axion-release version computation"
      via: "last tag v0.1.23 on main, so currentVersion is 0.1.24-SNAPSHOT"
      pattern: "0.1.24-SNAPSHOT"
  prohibitions:
    - statement: "MUST NOT push main, a tag or an image in this plan. Publishing waits for the user's explicit approval in 01-04 (D-05)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT discard, overwrite or stash-and-forget the user's uncommitted .serena/project.yml in the main worktree, or the user's uncommitted CLAUDE.md edits in this worktree, without the user's explicit choice"
      status: resolved
      verification: judgment
    - statement: "MUST NOT make the combined suite pass by deleting, @Disabled-ing, skipping or weakening tests"
      status: resolved
      verification: judgment
---

<objective>
Prove that the combined wave-1 tree (backend 01-01 plus frontend 01-02) builds, passes every test and runs end-to-end as the real app. Record the resolved versions in STACK.md. Then, per D-05, land `sbartram/main` on `main` with `--no-ff` in the main worktree, after the user decides how to handle that worktree's uncommitted `.serena/project.yml`.

Purpose: This is the integration gate between "each half works" and "publish". Nothing here is outward-facing. The merge into the local `main` is reversible until 01-04's approved push.

Output:
- a STACK.md docs commit on `sbartram/main`
- a local, unpushed `--no-ff` merge commit on `main`
- smoke evidence under `$HOME/.cache/myfeeder-phase01/`
</objective>

<execution_context>
@/Users/scottb/orca/workspaces/myfeeder/main/.claude/gsd-core/workflows/execute-plan.md
@/Users/scottb/orca/workspaces/myfeeder/main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-upgrade/01-CONTEXT.md
@.planning/phases/01-dependency-upgrade/01-RESEARCH.md
@.planning/phases/01-dependency-upgrade/01-01-SUMMARY.md
@.planning/phases/01-dependency-upgrade/01-02-SUMMARY.md
@.planning/codebase/STACK.md

The 01-01 and 01-02 SUMMARYs are referenced because this plan consumes their outputs: the backend baseline totals, the approved frontend versions, and any npm holds.

Environment facts (verified at planning time):
- The main worktree is `/Volumes/data2/scottb/dev/bartram/myfeeder`, on branch `main` at 02f74a1, which equals `origin/main`. Its only local change is ` M .serena/project.yml`. That working copy was byte-identical to the version committed on `sbartram/main` in commit 5aa00cc ("serena"), and `sbartram/main` changes that file relative to `main`. Git therefore refuses the merge while the local modification exists, and axion's release check needs a clean tree.
- This worktree (`/Users/scottb/orca/workspaces/myfeeder/main`) is on `sbartram/main`. `main` is an ancestor of it, so the merge is conflict-free by construction.
- Docker Desktop socket: `/Users/scottb/.docker/run/docker.sock`. Test runs need `DOCKER_HOST=unix:///Users/scottb/.docker/run/docker.sock` (the build.gradle.kts default points at a missing Rancher socket). `bootTestRun` is a JavaExec, not a Test task, so Ryuk stays enabled there and cleans up containers when the app JVM exits.
- `POST /api/feeds` takes `{"url": "...", "folderId": null}` and returns 201 with the saved feed (FeedService.subscribe fetches and parses through FeedFetcher). `POST /api/feeds/{id}/poll` forces a poll. `GET /api/articles?limit=N` returns `{items, nextCursor}`.
- The repo is public, so evidence stays in `$HOME/.cache/myfeeder-phase01/`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): The combined tree builds, passes every test and serves the upgraded app locally, one real end-to-end path</name>
  <files>none (verification only; build outputs go to the gitignored build/ and src/main/resources/static/)</files>
  <read_first>
    - build.gradle.kts (lines 90-122: test environment, npmInstall/npmBuild wiring into processResources)
    - src/test/java/org/bartram/myfeeder/TestMyfeederApplication.java and src/test/java/org/bartram/myfeeder/TestcontainersConfiguration.java (bootTestRun wiring, postgres:latest and redis:latest)
    - src/main/java/org/bartram/myfeeder/controller/FeedController.java and src/main/java/org/bartram/myfeeder/controller/SubscribeRequest.java (subscribe contract)
    - .planning/phases/01-dependency-upgrade/01-RESEARCH.md (Pitfall 1 on the Docker socket, Pitfall 7 on the stale frontend)
  </read_first>
  <action>
1. Docker: if `docker info` fails, run `open -a Docker` and poll `docker info` every 5s for up to 3 minutes. If it is still down, stop with a checkpoint:human-action asking the user to start Docker Desktop.

2. Record the commit being verified: `mkdir -p "$HOME/.cache/myfeeder-phase01" && git rev-parse HEAD > "$HOME/.cache/myfeeder-phase01/verified-sha.txt"`.

3. Full combined build: `DOCKER_HOST=unix:///Users/scottb/.docker/run/docker.sock ./gradlew clean build`. This runs npmInstall, npmBuild, compile, every test class and bootJar.
   - Every `build/test-results/test/TEST-*.xml` must be free of failure and error elements.
   - There must be at least 31 result files: the 30 pre-phase classes plus HttpClientConfigurationTest.
   - Compare the totals with 01-01's upgraded totals. The skipped count must be the same.
   - If a test fails, do not skip, @Disable, delete or weaken it. Diagnose it, fix it within the upgrade's scope, or stop and report.

4. The lockfile must be unchanged by the build: `git status --porcelain -- src/main/frontend/package.json src/main/frontend/package-lock.json` must print nothing.

5. Local end-to-end boot of the upgraded app:
   - Port 8080 must be free (`lsof -ti tcp:8080` prints nothing). If it is not, stop and report; do not kill a process you did not start.
   - Start in the background: `DOCKER_HOST=unix:///Users/scottb/.docker/run/docker.sock SPRING_AI_ANTHROPIC_API_KEY=test-dummy-key ./gradlew bootTestRun > "$HOME/.cache/myfeeder-phase01/boottestrun.log" 2>&1 &`. Use the dummy key; never the real Anthropic key.
   - Poll `curl -sf http://localhost:8080/actuator/health` until it reports `"status":"UP"`, for at most 4 minutes.

6. Smoke the real paths and save the evidence:
   - `curl -sf http://localhost:8080/api/version > "$HOME/.cache/myfeeder-phase01/smoke-version.json"`
   - `curl -sf http://localhost:8080/ > "$HOME/.cache/myfeeder-phase01/smoke-index.html"`. It must reference an `assets/index-` bundle, meaning the SPA is embedded from the fresh npmBuild.
   - `curl -sf -X POST http://localhost:8080/api/feeds -H 'Content-Type: application/json' -d '{"url":"https://spring.io/blog.atom"}' > "$HOME/.cache/myfeeder-phase01/smoke-subscribe.json"`. The response must be 201 with a non-empty title. This goes through FeedFetcher, the SSRF guard, the Reactor Netty transport with the bound timeouts, FeedParser (ROME / Jackson 3.1) and Postgres. If spring.io is unreachable, use another public Atom/RSS feed (for example https://github.blog/feed/) and record which one.
   - Trigger a poll with `curl -sf -X POST http://localhost:8080/api/feeds/ID/poll`, using the id from the subscribe response. Then save `curl -sf 'http://localhost:8080/api/articles?limit=5' > "$HOME/.cache/myfeeder-phase01/smoke-articles.json"`. It must contain at least one item; retry for up to 60s.

7. Stop the app:
   - Kill the process listening on 8080, then the background Gradle client.
   - Confirm port 8080 is free.
   - Confirm `docker ps --filter label=org.testcontainers=true -q` prints nothing after up to 30s. If containers from this run remain, remove them with `docker rm -f`.

8. Scan the boot log. It must contain the Spring Boot started line for MyfeederApplication (or TestMyfeederApplication), and no ERROR-level log line. Inspect any WARN about the subscribed feed and record it in the SUMMARY.
  </action>
  <verify>
    <automated>DOCKER_HOST=unix:///Users/scottb/.docker/run/docker.sock ./gradlew build</automated>
    <fails_when>non-zero exit or "BUILD FAILED" in the output (on a re-run the tasks are up to date, and a regression in the tree fails compile or test)</fails_when>
    <automated>n=$(find build/test-results/test -name 'TEST-*.xml' | wc -l) && test "$n" -ge 31 && test -f build/test-results/test/TEST-org.bartram.myfeeder.config.HttpClientConfigurationTest.xml && test -f build/test-results/test/TEST-org.bartram.myfeeder.MyfeederApplicationTests.xml && ! grep -lE '<(failure|error)[ >]' build/test-results/test/TEST-*.xml</automated>
    <fails_when>non-zero exit: fewer than 31 test result files, the new test or the Testcontainers context test did not run, or some result file contains a failure/error element</fails_when>
    <automated>test -z "$(git status --porcelain -- src/main/frontend/package.json src/main/frontend/package-lock.json)"</automated>
    <fails_when>non-zero exit: the Gradle npmInstall step rewrote the committed manifest or lockfile</fails_when>
    <automated>grep -qE 'Started (Test)?MyfeederApplication' "$HOME/.cache/myfeeder-phase01/boottestrun.log" && ! grep -q ' ERROR ' "$HOME/.cache/myfeeder-phase01/boottestrun.log" && grep -q '"version"' "$HOME/.cache/myfeeder-phase01/smoke-version.json" && grep -q 'assets/index-' "$HOME/.cache/myfeeder-phase01/smoke-index.html" && grep -qE '"title":"[^"]+' "$HOME/.cache/myfeeder-phase01/smoke-subscribe.json" && node -e 'const r=JSON.parse(require("fs").readFileSync(process.env.HOME+"/.cache/myfeeder-phase01/smoke-articles.json","utf8"));if(!(Array.isArray(r.items)&&r.items.length>0))process.exit(1)'</automated>
    <fails_when>non-zero exit: the app never logged its started line, an ERROR-level line appears in the boot log, /api/version or the SPA index was not served, the feed subscription returned no title, or no articles came back</fails_when>
  </verify>
  <acceptance_criteria>
    - `./gradlew clean build` (Docker) exits 0. There are at least 31 `TEST-*.xml` files with no failure or error elements, and the skipped count equals 01-01's upgraded run (recorded in the SUMMARY).
    - `git status --porcelain` for the two frontend manifest files is empty after the build.
    - These evidence files all exist: `boottestrun.log`, `smoke-version.json`, `smoke-index.html`, `smoke-subscribe.json` (with a non-empty title) and `smoke-articles.json` (at least one item). The SUMMARY records the feed URL used and the served bundle name.
    - `$HOME/.cache/myfeeder-phase01/verified-sha.txt` holds the SHA that was built and smoked.
    - After the task, port 8080 is free and no `org.testcontainers=true` containers remain.
  </acceptance_criteria>
  <done>The exact combined tree about to be merged builds, passes every test and runs as the real app locally, subscribing and reading a live feed through the upgraded HTTP stack.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Decide how to clear the main worktree's uncommitted .serena/project.yml before the merge (D-05)</name>
  <read_first>
    - .planning/phases/01-dependency-upgrade/01-CONTEXT.md (D-05)
    - .planning/phases/01-dependency-upgrade/01-RESEARCH.md (Pitfall 3 on axion's clean-tree check)
  </read_first>
  <decision>How should the uncommitted `.serena/project.yml` in the main worktree (`/Volumes/data2/scottb/dev/bartram/myfeeder`) be cleared, so that `git merge --no-ff sbartram/main` can run there and `./gradlew release` later sees a clean tree (D-05)?</decision>
  <context>
Before presenting this checkpoint, the executor runs the read-only check `git -C /Volumes/data2/scottb/dev/bartram/myfeeder diff --quiet sbartram/main -- .serena/project.yml` and reports the result:
- Exit 0: the working copy is byte-identical to the version the merge brings in (commit 5aa00cc on sbartram/main).
- Non-zero: it differs.

At planning time it was identical: Serena's regenerated language-server list was committed on the branch and also left uncommitted on main. Git refuses to merge while a file the merge changes has local modifications. The user's work is never discarded silently, which is why this is a blocking-human decision.
  </context>
  <options>
    <option id="restore">
      <name>Restore the file to HEAD, then merge (recommended if the check reported identical)</name>
      <pros>Nothing is lost, because the merge brings the byte-identical content back. The main worktree ends clean with no leftover stash entries.</pros>
      <cons>Only safe while the file is identical. The executor re-runs the identity check just before restoring and aborts this option if the content has diverged.</cons>
    </option>
    <option id="stash">
      <name>Keep a safety copy in a named stash, then merge</name>
      <pros>A copy survives even if the identity check is wrong or the file changes. It uses the named-stash protocol (`git stash push -m phase01-serena-project-yml -- .serena/project.yml`, with the entry SHA captured), and never a bare stash.</pros>
      <cons>It leaves a stash entry the user must drop later. The stash stack is shared across worktrees and sessions. The executor does not re-apply or drop it.</cons>
    </option>
    <option id="manual">
      <name>The user clears the file themselves</name>
      <pros>Full user control over their own uncommitted change.</pros>
      <cons>It needs manual action. The executor only verifies that `git -C /Volumes/data2/scottb/dev/bartram/myfeeder status --porcelain` is empty before merging.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The SUMMARY records the identity-check result that was presented and the user's choice verbatim.
  </acceptance_criteria>
  <resume-signal>Select: restore, stash, or manual (reply "done" once you have cleared it yourself)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the resolved versions in STACK.md, then land sbartram/main on main with --no-ff (unpushed)</name>
  <files>.planning/codebase/STACK.md</files>
  <read_first>
    - .planning/codebase/STACK.md (target lines: 42, 46, 57, 73, 78-84, 93-94, 107, 115, 122, 139, 154)
    - src/main/frontend/package-lock.json (installed versions from 01-02's commit)
    - $HOME/.cache/myfeeder-phase01/runtime-deps.txt (01-01's resolved backend tree)
    - $HOME/.cache/myfeeder-phase01/verified-sha.txt (Task 1)
  </read_first>
  <action>
1. STACK.md, editing in place and touching no other lines:
   - Lines 42 and 122: the Boot version becomes 4.0.8.
   - Line 46: the "Spring RestClient - HTTP client" bullet gains "(Reactor Netty transport via reactor-netty-http, D-01)".
   - Line 57: the Spring Cloud Resilience4j line becomes 2025.1.3.
   - Line 73: the Spring AI line becomes 2.0.1.
   - Line 154: the timeouts bullet now names `spring.http.clients.{connect-timeout: 5s, read-timeout: 30s}`.
   - Frontend lines 78-84, 93-94, 107, 115 and 139: set React, React Router, TanStack Query, Zustand, Vite, TypeScript, DOMPurify, Vitest, React Testing Library, ESLint and @vitejs/plugin-react to the versions installed per the committed lockfile (`packages["node_modules/NAME"].version`). Line 139 uses major.minor only.

   Per D-03, do not change any other version text. Commit `.planning/codebase/STACK.md` alone, staged by explicit path: `docs(01-03): STACK.md records upgraded dependency versions`.

2. Code-unchanged guard: `git diff --quiet "$(cat "$HOME/.cache/myfeeder-phase01/verified-sha.txt")" sbartram/main -- src build.gradle.kts gradle Dockerfile helm deploy.sh`. It must exit 0, meaning no code changed since Task 1 verified it.

3. Prepare the main worktree (MAIN = `/Volumes/data2/scottb/dev/bartram/myfeeder`):
   - `git -C MAIN rev-parse --abbrev-ref HEAD` must print `main`.
   - Run `git -C MAIN fetch origin`. `git -C MAIN rev-parse main` must equal `git -C MAIN rev-parse origin/main`. If main already has unpushed commits, stop and report.
   - Apply the Task 2 choice:
     - restore: re-run the identity check `git -C MAIN diff --quiet sbartram/main -- .serena/project.yml`. Only if it exits 0, run `git -C MAIN restore .serena/project.yml`. Otherwise stop and re-raise the decision, because the content diverged.
     - stash: run `git -C MAIN stash push -m phase01-serena-project-yml -- .serena/project.yml`. Immediately capture the entry SHA from `git -C MAIN stash list --format='%H %gs'` and record it in the SUMMARY. Do not re-apply or drop it.
     - manual: only verify.
   - `git -C MAIN status --porcelain` must then print nothing.

4. Merge per D-05: `git -C MAIN merge --no-ff sbartram/main -m "Merge branch 'sbartram/main': Phase 1 dependency upgrade (Boot 4.0.8, Spring AI 2.0.1, Spring Cloud 2025.1.3, frontend in-major refresh)"`.
   - If a conflict or a hook failure occurs, run `git -C MAIN merge --abort` and stop.
   - Never `git checkout <sha>` in either worktree.
   - Do NOT push. The push is 01-04's approved step.

5. Post-merge checks are in the verify block: the merge commit, tree equality with main^2, a clean status, main unpushed, and axion reading 0.1.24-SNAPSHOT.
  </action>
  <verify>
    <automated>grep -q 'Spring Boot 4.0.8' .planning/codebase/STACK.md && ! grep -q 'Spring Boot 4.0.3' .planning/codebase/STACK.md && grep -q 'Spring AI 2.0.1' .planning/codebase/STACK.md && grep -q 'Resilience4j 2025.1.3' .planning/codebase/STACK.md && grep -q 'spring.http.clients' .planning/codebase/STACK.md && grep -q 'reactor-netty-http' .planning/codebase/STACK.md</automated>
    <fails_when>non-zero exit: a backend version, the timeout key prefix or the transport note was not updated in STACK.md, or the old Boot version text remains</fails_when>
    <automated>git -C /Volumes/data2/scottb/dev/bartram/myfeeder rev-parse --verify --quiet main^2 > /dev/null && git -C /Volumes/data2/scottb/dev/bartram/myfeeder diff --quiet main^2 main && git -C /Volumes/data2/scottb/dev/bartram/myfeeder diff --quiet "$(cat "$HOME/.cache/myfeeder-phase01/verified-sha.txt")" main -- src build.gradle.kts gradle Dockerfile helm deploy.sh && test -z "$(git -C /Volumes/data2/scottb/dev/bartram/myfeeder status --porcelain)"</automated>
    <fails_when>non-zero exit: main's tip is not a merge commit, the merge tree differs from the merged branch tip, the code paths differ from the Task 1 verified commit, or the main worktree is dirty</fails_when>
    <automated>git -C /Volumes/data2/scottb/dev/bartram/myfeeder merge-base --is-ancestor origin/main main && ! git -C /Volumes/data2/scottb/dev/bartram/myfeeder merge-base --is-ancestor main origin/main</automated>
    <fails_when>non-zero exit: main diverged from origin/main, or main was already pushed (it must stay unpushed until the 01-04 approval)</fails_when>
    <automated>(cd /Volumes/data2/scottb/dev/bartram/myfeeder && ./gradlew currentVersion -q) > "$HOME/.cache/myfeeder-phase01/main-version.txt" && grep -q 'Project version: 0.1.24-SNAPSHOT' "$HOME/.cache/myfeeder-phase01/main-version.txt"</automated>
    <fails_when>non-zero exit: axion does not compute 0.1.24-SNAPSHOT on main (unexpected tag history or a dirty tree), so the release would not be 0.1.24</fails_when>
  </verify>
  <acceptance_criteria>
    - STACK.md shows Spring Boot 4.0.8 (no 4.0.3 left), Spring AI 2.0.1, Resilience4j 2025.1.3, `spring.http.clients`, `reactor-netty-http`, and frontend versions matching the committed lockfile. The STACK.md commit contains only that file.
    - In the main worktree, `main` is a two-parent merge commit whose second parent is the `sbartram/main` tip. `git diff --quiet main^2 main` exits 0, and the code paths equal `verified-sha.txt`.
    - `git -C /Volumes/data2/scottb/dev/bartram/myfeeder status --porcelain` is empty, and `.serena/project.yml` was handled exactly as the user chose (with the stash SHA recorded if they chose stash).
    - `origin/main` is a strict ancestor of `main`. Nothing was pushed.
    - `main-version.txt` contains `Project version: 0.1.24-SNAPSHOT`.
  </acceptance_criteria>
  <done>STACK.md is current, and the verified tree is merged into the local main with --no-ff, clean, unpushed, with axion ready to cut 0.1.24.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build ↔ Docker daemon (Testcontainers) | Test and smoke containers run from floating `postgres:latest` / `redis:latest` images |
| Local app → public internet (smoke subscription) | One outbound fetch of a public feed through the production FeedFetcher path |
| Feature branch → main (local merge) | Code verified here becomes the release candidate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Information Disclosure | Local bootTestRun smoke (Anthropic API key) | low | mitigate | The smoke run sets `SPRING_AI_ANTHROPIC_API_KEY=test-dummy-key`. The real key is never exported into the local run, and no code path calls Anthropic. |
| T-01-09 | Tampering | Merge of sbartram/main into main | medium | mitigate | Merge only after the Task 1 build and smoke pass. The code-path diff against `verified-sha.txt` must be empty. The merge tree must equal main^2. Nothing is pushed until the 01-04 blocking-human approval. |
| T-01-10 | Tampering | User's uncommitted .serena/project.yml (main worktree) | low | mitigate | Blocking-human decision. `restore` is allowed only after a byte-identity check against the incoming content. `stash` uses a named, SHA-captured entry. No bare stash, no silent checkout. |
| T-01-11 | Elevation of Privilege | Smoke subscription through FeedFetcher (SSRF surface) | low | accept | The smoke target is a fixed public HTTPS feed, and FeedUrlValidator still runs. The run is local, short-lived, and uses Testcontainers-only data. |
</threat_model>

<verification>
- The Task 1 tracer covers the full combined build (Docker), 31 or more green result files, an unchanged lockfile, and the local boot smoke (health UP, version, SPA, a real feed subscription, articles, no ERROR-level log lines).
- Task 2: the user's .serena decision is recorded.
- Task 3: the STACK.md docs commit, the code-unchanged guard, a clean main worktree, the `--no-ff` merge with tree equality, main unpushed, and axion at 0.1.24-SNAPSHOT.
</verification>

<success_criteria>
- UPG-01 and UPG-02 hold together on one tree, not just separately.
- UPG-03 prerequisites: the release candidate is merged into the local `main` with the user's decision on their uncommitted file (D-05), and it is ready for the approved push in 01-04.
</success_criteria>

## Artifacts this phase produces

| Artifact | Produced by | Notes |
|----------|-------------|-------|
| Backend upgrade files and root CLAUDE.md lines | 01-01 | |
| Frontend manifest and lockfile | 01-02 | |
| `.planning/codebase/STACK.md` (resolved versions) | **01-03 (this plan)** | Docs commit on sbartram/main |
| `--no-ff` merge commit on local `main` | **01-03 (this plan)** | Unpushed until 01-04 |
| Named stash `phase01-serena-project-yml` in the shared stash stack | **01-03 (this plan)**, only if the user picks `stash` | The user owns and drops it |
| Tag `v0.1.24`, image `:0.1.24`, Helm revision on k3s | 01-04 | |

New symbols created by this plan (for source-grounding / drift exclusion): none in source code. The evidence files are `verified-sha.txt`, `boottestrun.log`, `smoke-version.json`, `smoke-index.html`, `smoke-subscribe.json`, `smoke-articles.json` and `main-version.txt`, all under `$HOME/.cache/myfeeder-phase01/`.

<output>
Create `.planning/phases/01-dependency-upgrade/01-03-SUMMARY.md` when done. Include the combined-build totals, the smoke evidence summary (the feed URL used, the bundle name, the article count), the .serena identity-check result and the user's choice, the STACK.md commit SHA and the merge commit SHA.
</output>
