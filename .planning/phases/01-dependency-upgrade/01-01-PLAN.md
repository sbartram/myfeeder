---
phase: 01-dependency-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java
  - src/main/resources/application.yaml
  - CLAUDE.md
autonomous: true
requirements: [UPG-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "build.gradle.kts declares the Spring Boot plugin at 4.0.8, springAiVersion 2.0.1 and springCloudVersion 2025.1.3 (UPG-01)"
    - "Per D-03 nothing outside the stated targets moves: axion-release stays 1.21.1, the Gradle wrapper stays 9.4.1, and rome 2.1.0, rome-modules 2.1.0 and readability4j 1.0.8 are unchanged"
    - "Edge UPG-01/adjacency: where Spring AI 2.0.1 asks for Spring Boot 4.1.1 and the Boot plugin manages 4.0.8, the resolved runtimeClasspath uses 4.0.8, so no org.springframework.boot artifact resolves to 4.1.x"
    - "Edge UPG-01/ordering: the build.gradle.kts diff touches only the Boot plugin version line, the two extra[...] version lines and the added reactor-netty-http lines. The dependencyManagement imports block and its BOM order (Spring AI, then Spring Cloud) are byte-identical"
    - "Per D-01 the auto-configured outbound HTTP client stays Reactor Netty. The ClientHttpRequestFactory from Boot's imperative HTTP-client auto-configuration is ReactorClientHttpRequestFactory, which HttpClientConfigurationTest proves. That test was seen failing (JdkClientHttpRequestFactory) before the dependency was added"
    - "Per D-01 reactor-netty-http is declared in build.gradle.kts with no version (the Boot BOM manages it), and spring-webflux is absent from runtimeClasspath"
    - "Per D-02 and edge UPG-01/empty: spring.http.clients.connect-timeout 5s and read-timeout 30s in src/main/resources/application.yaml bind to non-null HttpClientSettings values. A test that loads the main yaml proves it and was seen failing (null) before the key rename"
    - "Per D-02 the timeout-key fix is its own commit, separate from the version-bump commit"
    - "Every backend test passes on the upgraded tree with Docker-backed Testcontainers. The skipped count equals the pre-upgrade baseline and the class count is the baseline plus the new HttpClientConfigurationTest (UPG-01)"
    - "Root CLAUDE.md says Spring Boot 4.0.8, names spring.http.clients.* as the bound timeout keys (D-02), and says the outbound RestClient transport is Reactor Netty (D-01)"
    - "The user's pre-existing uncommitted CLAUDE.md hunks are still uncommitted and unchanged after this plan, and nothing under .claude/ is staged or committed"
  artifacts:
    - path: "build.gradle.kts"
      provides: "Target BOM versions plus the explicit Reactor Netty transport dependency"
      contains: "id(\"org.springframework.boot\") version \"4.0.8\""
    - path: "src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java"
      provides: "Docker-free regression tests for the outbound transport (D-01) and timeout binding (D-02)"
      contains: "ReactorClientHttpRequestFactory"
    - path: "src/main/resources/application.yaml"
      provides: "Outbound timeouts under the bound spring.http.clients prefix"
      contains: "clients:"
    - path: "CLAUDE.md"
      provides: "Docs matching the upgraded stack, transport and timeout keys"
      contains: "spring.http.clients.connect-timeout"
  key_links:
    - from: "build.gradle.kts"
      to: "Boot HTTP client detection (ClientHttpRequestFactoryBuilder.detect)"
      via: "reactor-netty-http on the runtime classpath makes Boot pick the Reactor factory for the auto-configured RestClient.Builder"
      pattern: "implementation\\(\"io\\.projectreactor\\.netty:reactor-netty-http\"\\)"
    - from: "src/main/resources/application.yaml"
      to: "HttpClientSettings"
      via: "spring.http.clients.* bound by Boot's HttpClientsProperties"
      pattern: "^    clients:$"
    - from: "src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java"
      to: "src/main/resources/application.yaml"
      via: "YamlPropertySourceLoader over a FileSystemResource (the test-classpath application.yaml shadows the main one)"
      pattern: "src/main/resources/application.yaml"
  prohibitions:
    - statement: "MUST NOT make the backend suite pass by deleting, @Disabled-ing, skipping, or weakening existing tests or assertions"
      status: resolved
      verification: judgment
    - statement: "MUST NOT commit, discard, or stash-and-forget the user's pre-existing uncommitted edits in CLAUDE.md or .claude/CLAUDE.md, and MUST NOT stage the untracked .claude/ GSD install"
      status: resolved
      verification: judgment
---

<objective>
Move the backend to Spring Boot 4.0.8, the Spring AI BOM 2.0.1 and Spring Cloud 2025.1.3 (UPG-01) so the app behaves exactly as before. Per D-01, keep the outbound HTTP transport on Reactor Netty. Per D-02, make the documented 5s/30s outbound timeouts real by binding them under the non-deprecated key prefix. Per D-03, touch nothing outside the stated targets. Correct root CLAUDE.md to match.

Purpose: This is the backend half of the phase. Spring AI 2.0.1 drops the reactor-netty jar that 2.0.0-M2 brought in transitively. A bare version bump would therefore silently move every feed fetch, Raindrop call and reader-view extraction onto the JDK HTTP/2 client, and no existing test would notice, because MockRestServiceServer replaces the request factory. This plan pins the transport and adds a Docker-free test that fails if it ever changes again.

Output: three commits: the version bump plus transport test, the D-02 key fix plus timeout test, and the CLAUDE.md docs commit. Plus a recorded backend baseline in `$HOME/.cache/myfeeder-phase01/`.
</objective>

<execution_context>
@/Users/scottb/orca/workspaces/myfeeder/main/.claude/gsd-core/workflows/execute-plan.md
@/Users/scottb/orca/workspaces/myfeeder/main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-upgrade/01-CONTEXT.md
@.planning/phases/01-dependency-upgrade/01-RESEARCH.md
@.planning/phases/01-dependency-upgrade/01-PATTERNS.md
@build.gradle.kts
@src/main/resources/application.yaml
@src/main/java/org/bartram/myfeeder/config/RestClientConfig.java
@src/test/java/org/bartram/myfeeder/controller/VersionControllerTest.java

Environment facts (verified at planning time):
- Docker Desktop is installed but was not running. Its socket is `/Users/scottb/.docker/run/docker.sock`, and `/var/run/docker.sock` symlinks to it. build.gradle.kts line 92 defaults `DOCKER_HOST` to a Rancher Desktop path that does not exist, so every Testcontainers run must prefix `DOCKER_HOST=unix:///Users/scottb/.docker/run/docker.sock`. Do not edit line 92; that is out of scope per D-03.
- Plan 01-02 runs in parallel and owns `src/main/frontend/`. Every Gradle command in this plan passes `-x npmBuild -x npmInstall` so it never runs npm against a directory that plan 01-02 is rewriting.
- Root `CLAUDE.md` and `.claude/CLAUDE.md` may carry uncommitted user edits that are not phase work. At planning time the root CLAUDE.md hunks were at lines 40, 55-56, 99 and 105, all line-count-neutral. `git add -p` is interactive and not available to executors. Task 3 gives a non-interactive way to stage only this plan's lines.
- The repo `sbartram/myfeeder` is public. Evidence files go under `$HOME/.cache/myfeeder-phase01/`, not into the repo.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Upgraded backend resolves, keeps Reactor Netty and passes the full Docker-backed suite, one path end-to-end</name>
  <files>build.gradle.kts, src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java</files>
  <read_first>
    - build.gradle.kts (the whole file: lines 3, 28-29 change and one line is added after line 36; lines 5, 68-73, 75-95 must stay byte-identical)
    - .planning/phases/01-dependency-upgrade/01-PATTERNS.md (the "HttpClientConfigurationTest" section: runner shape, class names verified from the Boot 4.0.8 jars, and the test-yaml shadowing pitfall)
    - .planning/phases/01-dependency-upgrade/01-RESEARCH.md (the "Decisions for the Planner" D-1 transport finding, Pitfall 1 on the Docker socket, and the Validation Architecture section)
    - src/test/java/org/bartram/myfeeder/controller/VersionControllerTest.java (test conventions: package-private class, AssertJ, 4-space indent)
    - src/main/java/org/bartram/myfeeder/config/RestClientConfig.java (the User-Agent customizer; the transport must be pinned by dependency, never by hand-building a request factory)
  </read_first>
  <action>
Run these steps in this order. Stop and report if any step fails in a way these steps do not handle.

1. Docker: if `docker info` fails, run `open -a Docker`, then poll `docker info` every 5 seconds for up to 3 minutes. If the daemon is still down, stop and raise a checkpoint:human-action asking the user to start Docker Desktop.

2. Baseline, before editing any file: run `mkdir -p "$HOME/.cache/myfeeder-phase01"`. Then run `DOCKER_HOST=unix:///Users/scottb/.docker/run/docker.sock ./gradlew cleanTest test -x npmBuild -x npmInstall` on the unchanged tree (Boot 4.0.3). `cleanTest` forces a real re-run. From `build/test-results/test/TEST-*.xml`, record per-class tests, skipped, failures and errors, plus the totals, in `$HOME/.cache/myfeeder-phase01/backend-baseline.txt`. A baseline failure is data, not a stop condition, but keep it for step 7.

3. Edit exactly three lines of build.gradle.kts (UPG-01):
   - line 3: the `org.springframework.boot` plugin version becomes "4.0.8"
   - line 28: `extra["springAiVersion"]` becomes "2.0.1"
   - line 29: `extra["springCloudVersion"]` becomes "2025.1.3"

   Per D-03, leave everything else alone: the axion-release plugin line (1.21.1), the rome, rome-modules and readability4j coordinates, the `dependencyManagement` block and its import order, the release/pushRelease/gitPushRelease block, and the `DOCKER_HOST` default. Never add a version to any Boot, Spring AI or Spring Cloud managed artifact, and never override Jackson, Micrometer or Spring versions to the 4.1.x line that Spring AI 2.0.1 was compiled against.

4. Create `src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java`. It uses package `org.bartram.myfeeder.config` (the test directory is new) and is a package-private class. Give it a field `ApplicationContextRunner` built with `AutoConfigurations.of(HttpClientAutoConfiguration.class, ImperativeHttpClientAutoConfiguration.class)`. Those come from `org.springframework.boot.http.client.autoconfigure` and `org.springframework.boot.http.client.autoconfigure.imperative`, verified in the 4.0.8 jars.

   Add test `outboundTransportIsReactorNetty`. It asserts, with AssertJ `assertThat(...).isInstanceOf(...)`, that the context's `ClientHttpRequestFactory` bean is an `org.springframework.http.client.ReactorClientHttpRequestFactory`.

   Confirm two details at compile time:
   - the package of `AutoConfigurations` (expected `org.springframework.boot.autoconfigure.AutoConfigurations`)
   - whether `ImperativeHttpClientAutoConfiguration` carries a condition the runner must satisfy

   If the 4.0.8 auto-configuration does not expose a `ClientHttpRequestFactory` bean, assert instead that the `ClientHttpRequestFactoryBuilder` bean is a `ReactorClientHttpRequestFactoryBuilder`.

   Do not use `@SpringBootTest`, because it needs Docker. Do not use MockRestServiceServer, because it replaces the factory and hides the transport.

5. RED: run `./gradlew test -x npmBuild -x npmInstall --tests 'org.bartram.myfeeder.config.HttpClientConfigurationTest'`. It MUST fail and report a JDK request factory. That proves the test sees the transport switch that the bare upgrade causes. If it passes at this point, the test is not discriminating; fix the test before going on. Record the failure message for the SUMMARY.

6. Per D-01, add `implementation("io.projectreactor.netty:reactor-netty-http")` directly after the `spring-boot-starter-restclient` line (currently line 36). Use tab indentation and no version string, because the Boot BOM manages it (1.3.7 on 4.0.8 per research). Put a one-line `//` comment above it saying it pins the auto-configured RestClient transport to Reactor Netty, which Spring AI 2.0.0-M2 used to bring in transitively. Re-run the step-5 command. It must now pass (GREEN).

7. Full suite on the upgraded tree: `DOCKER_HOST=unix:///Users/scottb/.docker/run/docker.sock ./gradlew cleanTest test -x npmBuild -x npmInstall`. Compare it with the baseline:
   - failures and errors must be zero
   - the skipped total must equal the baseline
   - there must be exactly one more test class than the baseline (the new one)

   If a test that already failed in the baseline fails again, it is pre-existing, for example the floating `postgres:latest` or `redis:latest` images. Do NOT modify, skip or @Disable it. Stop and report it as a blocker, because UPG-01 requires every test to pass.

8. Commit only the two files, staging each by explicit path. Never use `git add -A`, `git add .` or `commit -a`, and never stage anything under `.claude/`. Commit message: `build(01-01): upgrade to Spring Boot 4.0.8, Spring AI 2.0.1, Spring Cloud 2025.1.3; keep Reactor Netty transport (D-01)`.
  </action>
  <verify>
    <automated>grep -q 'id("org.springframework.boot") version "4.0.8"' build.gradle.kts && grep -q 'extra\["springAiVersion"\] = "2.0.1"' build.gradle.kts && grep -q 'extra\["springCloudVersion"\] = "2025.1.3"' build.gradle.kts && grep -q 'implementation("io.projectreactor.netty:reactor-netty-http")' build.gradle.kts && grep -q 'axion-release") version "1.21.1"' build.gradle.kts && grep -q 'gradle-9.4.1-bin.zip' gradle/wrapper/gradle-wrapper.properties</automated>
    <fails_when>non-zero exit: a target version string or the version-less reactor-netty-http line is missing, or axion-release/the Gradle wrapper moved off 1.21.1/9.4.1</fails_when>
    <automated>mkdir -p "$HOME/.cache/myfeeder-phase01" && ./gradlew -q dependencies --configuration runtimeClasspath > "$HOME/.cache/myfeeder-phase01/runtime-deps.txt" && grep -q 'org.springframework.boot:spring-boot:4.0.8' "$HOME/.cache/myfeeder-phase01/runtime-deps.txt" && grep -q 'org.springframework.ai:spring-ai-anthropic:2.0.1' "$HOME/.cache/myfeeder-phase01/runtime-deps.txt" && grep -q 'org.springframework.cloud:spring-cloud-commons:5.0.3' "$HOME/.cache/myfeeder-phase01/runtime-deps.txt" && grep -q 'io.projectreactor.netty:reactor-netty-http' "$HOME/.cache/myfeeder-phase01/runtime-deps.txt" && ! grep -q 'org.springframework:spring-webflux' "$HOME/.cache/myfeeder-phase01/runtime-deps.txt" && ! grep -qE 'org\.springframework\.boot:spring-boot[a-z-]*(:[0-9.]+)?( -> |:)4\.1\.[0-9]+( \([*c]\))?$' "$HOME/.cache/myfeeder-phase01/runtime-deps.txt"</automated>
    <fails_when>non-zero exit: spring-boot 4.0.8, spring-ai-anthropic 2.0.1, spring-cloud-commons 5.0.3 or reactor-netty-http is absent from the resolved tree, spring-webflux is present, or some org.springframework.boot artifact resolves to a 4.1.x version</fails_when>
    <automated>DOCKER_HOST=unix:///Users/scottb/.docker/run/docker.sock ./gradlew cleanTest test -x npmBuild -x npmInstall</automated>
    <fails_when>non-zero exit, "BUILD FAILED" in the output, or a "tests completed, N failed" line</fails_when>
    <automated>test -f build/test-results/test/TEST-org.bartram.myfeeder.config.HttpClientConfigurationTest.xml && test -f build/test-results/test/TEST-org.bartram.myfeeder.MyfeederApplicationTests.xml && ! grep -lE '<(failure|error)[ >]' build/test-results/test/TEST-*.xml</automated>
    <fails_when>non-zero exit: the new test or the Docker-backed context test produced no result file (the suite did not really run against Docker), or some result file lists a failure/error element</fails_when>
  </verify>
  <acceptance_criteria>
    - build.gradle.kts contains `id("org.springframework.boot") version "4.0.8"`, `extra["springAiVersion"] = "2.0.1"`, `extra["springCloudVersion"] = "2025.1.3"` and `implementation("io.projectreactor.netty:reactor-netty-http")`. It still contains `axion-release") version "1.21.1"`, `com.rometools:rome:2.1.0`, `com.rometools:rome-modules:2.1.0` and `readability4j:1.0.8`.
    - In the bump commit, the build.gradle.kts hunks have removed lines only for the three version lines. All added lines are the three new version lines, the reactor-netty-http line and its one comment line. The `dependencyManagement` block is unchanged.
    - The bump commit's file list is exactly `build.gradle.kts` and `src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java`.
    - The SUMMARY records the RED result (a JDK request factory reported) before the dependency was added, and the GREEN result after.
    - `$HOME/.cache/myfeeder-phase01/backend-baseline.txt` exists. The SUMMARY gives baseline totals and upgraded totals (tests, skipped, failures, errors), with equal skipped counts and zero upgraded failures and errors.
  </acceptance_criteria>
  <done>The upgraded backend resolves on the 4.0.8 / 2.0.1 / 2025.1.3 line with Reactor Netty still the outbound transport, the full Docker-backed suite is green against a recorded baseline, and the bump is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Outbound timeouts in application.yaml actually bind (D-02, own commit)</name>
  <files>src/main/resources/application.yaml, src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java</files>
  <read_first>
    - src/main/resources/application.yaml (lines 1-9: the comment on lines 4-5 and the timeout block on lines 6-9)
    - src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java (created in Task 1)
    - src/test/resources/application.yaml (it shadows the main yaml on the test classpath, which is why the test loads the main file from disk)
    - .planning/phases/01-dependency-upgrade/01-PATTERNS.md ("Critical pitfall: the test application.yaml shadows the main one")
  </read_first>
  <behavior>
    - Test outboundTimeoutsFromMainApplicationYamlBind: when src/main/resources/application.yaml is loaded into the HTTP-client auto-configuration context, HttpClientSettings.connectTimeout() equals 5 seconds and readTimeout() equals 30 seconds
    - On the current yaml the test fails, because keys under the deprecated singular prefix are not bound (both values are null)
    - After the rename the test passes, and outboundTransportIsReactorNetty still passes
  </behavior>
  <action>
Per D-02, and in a commit separate from the Task 1 version bump:

1. RED: add test method `outboundTimeoutsFromMainApplicationYamlBind` to HttpClientConfigurationTest.
   - Load the main yaml with `new org.springframework.boot.env.YamlPropertySourceLoader().load("main-application-yaml", new org.springframework.core.io.FileSystemResource("src/main/resources/application.yaml"))`. Use a FileSystemResource because `src/test/resources/application.yaml` shadows the classpath resource. Gradle runs tests with the project directory as the working directory.
   - Add the first returned PropertySource to the runner through `withInitializer(ctx -> ctx.getEnvironment().getPropertySources().addFirst(...))`.
   - Assert that the context's `org.springframework.boot.http.client.HttpClientSettings` bean has `connectTimeout()` equal to `Duration.ofSeconds(5)` and `readTimeout()` equal to `Duration.ofSeconds(30)`.
   - Confirm the YamlPropertySourceLoader package at compile time.
   - Run `./gradlew test -x npmBuild -x npmInstall --tests 'org.bartram.myfeeder.config.HttpClientConfigurationTest'`. The new test must FAIL with null values. Record that for the SUMMARY.

2. GREEN: in `src/main/resources/application.yaml`, rename the `client:` key under `spring.http` (line 7) to `clients:`.
   - Keep `connect-timeout: 5s` and `read-timeout: 30s` at the same indentation, with two-space YAML indentation.
   - Reword the comment on line 5. It wrongly names the JDK client as the transport, but under D-01 the transport is Reactor Netty. The new wording should say that without these settings the HTTP client waits indefinitely.
   - Change nothing else in the file.
   - Re-run the test class. Both tests must pass.

3. Run the Docker-free quick suite (the second automated command below). It must pass.

4. Commit exactly `src/main/resources/application.yaml` and `src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java`, staged by explicit path, with the message `fix(01-01): bind outbound HTTP timeouts via spring.http.clients.* (D-02)`.

Note for the SUMMARY: this is the one intentional behavior change in the phase. A feed slower than 30s now fails with a timeout instead of hanging a scheduler thread. Baseline polls take about 0.1-2s.
  </action>
  <verify>
    <automated>grep -qE '^    clients:$' src/main/resources/application.yaml && ! grep -qE '^    client:$' src/main/resources/application.yaml && grep -qE '^      connect-timeout: 5s$' src/main/resources/application.yaml && grep -qE '^      read-timeout: 30s$' src/main/resources/application.yaml && ! grep -q 'Without these the JDK client' src/main/resources/application.yaml</automated>
    <fails_when>non-zero exit: the plural key is missing, the deprecated singular key is still present, a timeout value or indentation changed, or the stale JDK-client comment remains</fails_when>
    <automated>./gradlew test -x npmBuild -x npmInstall --tests 'org.bartram.myfeeder.controller.*' --tests 'org.bartram.myfeeder.service.*' --tests 'org.bartram.myfeeder.parser.*' --tests 'org.bartram.myfeeder.integration.*' --tests 'org.bartram.myfeeder.scheduler.*' --tests 'org.bartram.myfeeder.config.*'</automated>
    <fails_when>non-zero exit, "No tests found for given includes", or a failed-test line in the output</fails_when>
    <automated>grep -q 'name="outboundTimeoutsFromMainApplicationYamlBind' build/test-results/test/TEST-org.bartram.myfeeder.config.HttpClientConfigurationTest.xml && grep -q 'name="outboundTransportIsReactorNetty' build/test-results/test/TEST-org.bartram.myfeeder.config.HttpClientConfigurationTest.xml && ! grep -qE '<(failure|error)[ >]' build/test-results/test/TEST-org.bartram.myfeeder.config.HttpClientConfigurationTest.xml</automated>
    <fails_when>non-zero exit: either test method is missing from the report or the report contains a failure/error element</fails_when>
    <automated>test "$(git log -1 --format=%H -- src/main/resources/application.yaml)" != "$(git log -1 --format=%H -- build.gradle.kts)"</automated>
    <fails_when>non-zero exit: the yaml fix landed in the same commit as the version bump</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/main/resources/application.yaml` has `    clients:` (4 spaces) under `  http:`, with `      connect-timeout: 5s` and `      read-timeout: 30s` below it. No other line of the file changed except the line-5 comment.
    - HttpClientConfigurationTest contains `outboundTimeoutsFromMainApplicationYamlBind` and `outboundTransportIsReactorNetty`. Both pass, and the SUMMARY records the RED run (null timeouts) before the rename.
    - The latest commit touching application.yaml lists exactly `src/main/resources/application.yaml` and the test file, and it is a different commit from the build.gradle.kts bump.
  </acceptance_criteria>
  <done>The documented 5s/30s outbound timeouts are bound for real, a test guards the main yaml's key names, and the fix is its own commit.</done>
</task>

<task type="auto">
  <name>Task 3: Root CLAUDE.md reflects Boot 4.0.8, the Reactor Netty transport and the bound timeout keys (stage only these lines)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines 1-40 and 140-170; the four target lines are 7, 36, 149 and 168)
    - .planning/phases/01-dependency-upgrade/01-PATTERNS.md (the "CLAUDE.md (root docs)" section and "Commit hygiene")
    - .planning/phases/01-dependency-upgrade/01-CONTEXT.md (D-01 and D-02 wording on what the docs must say)
  </read_first>
  <action>
1. Snapshot any pre-existing user hunks: `git diff -U0 -- CLAUDE.md > "$HOME/.cache/myfeeder-phase01/claude-md-user-hunks.before.diff"`. In an isolated worktree this may be empty. Check that every hunk header in the snapshot has equal old and new line counts. At planning time they were `-40 +40`, `-55,2 +55,2`, `-99 +99` and `-105 +105`, so line numbers match between HEAD and the working copy. If any hunk is not line-count-neutral, stop and report.

2. Edit the working copy of CLAUDE.md. Each edit replaces exactly one line in place, so no line numbers shift:
   - Line 7: the opening sentence says the app is a Spring Boot 4.0.8 feed aggregator. Change only the version number.
   - Line 36: the Framework bullet reads "Spring Boot 4.0.8 with Spring MVC (servlet stack)". Change only the version number.
   - Line 149 (D-02): rewrite the outbound-timeouts bullet so it says:
     - timeouts and redirects for the auto-configured RestClient are set via `spring.http.clients.connect-timeout` / `read-timeout` / `redirects`, and the metadata lives in spring-boot-http-client-*.jar
     - the singular-prefix keys were deprecated in Boot 4.0.0 and are silently not bound, so the old claim was false until this phase
     - the settings apply to every RestClient (feed fetches and Raindrop)
     - they are currently 5s/30s in `application.yaml`, guarded by `HttpClientConfigurationTest`
   - Line 168 (D-01): rewrite the Outbound RestClient User-Agent gotcha. Remove the sentence that attributes the default User-Agent to the JDK HttpClient. Say instead:
     - the auto-configured RestClient transport is Reactor Netty (`ReactorClientHttpRequestFactory`), pinned by the explicit `reactor-netty-http` dependency in build.gradle.kts and guarded by `HttpClientConfigurationTest`
     - without myfeeder's customizer, requests carry the HTTP library's default User-Agent, which Vercel and other CDNs rate-limit by returning HTTP 200 with body `{"data":"too many requests"}`
   - Keep the rest of the line-168 bullet: `config/RestClientConfig.java` registers a RestClientCustomizer that sets `myfeeder/<version>`, and code must not bypass it with a raw `RestClient.builder()`.
   - Keep the file's existing Markdown style: bold bullet lead-ins and backticked identifiers.

3. Stage only these four lines. `git add -p` is interactive and not available.
   - Run `git show HEAD:CLAUDE.md > "$HOME/.cache/myfeeder-phase01/CLAUDE.head.md"`.
   - Build `CLAUDE.staged.md` from it, replacing only lines 7, 36, 149 and 168 with the same-numbered lines from the working copy. An awk two-file line splice works.
   - Run `blob=$(git hash-object -w "$HOME/.cache/myfeeder-phase01/CLAUDE.staged.md")`, then `git update-index --cacheinfo 100644,"$blob",CLAUDE.md`.
   - Check that `git diff --cached --stat` lists only CLAUDE.md with 4 insertions and 4 deletions.
   - Never run `git add CLAUDE.md`, `git add -A` or `commit -a`, and never stage `.claude/CLAUDE.md` or anything under `.claude/`.

4. Commit with `docs(01-01): CLAUDE.md for Boot 4.0.8, Reactor Netty transport (D-01), spring.http.clients timeouts (D-02)`.

5. Re-run the snapshot command into `claude-md-user-hunks.after.diff`. Ignoring `index` lines, it must equal the before snapshot. That shows the user's hunks are untouched and still uncommitted, and that no phase edit was left unstaged.
  </action>
  <verify>
    <automated>git show HEAD:CLAUDE.md > "$HOME/.cache/myfeeder-phase01/CLAUDE.committed.md" && grep -q 'Spring Boot 4.0.8 feed aggregator' "$HOME/.cache/myfeeder-phase01/CLAUDE.committed.md" && grep -q 'Spring Boot 4.0.8 with Spring MVC' "$HOME/.cache/myfeeder-phase01/CLAUDE.committed.md" && grep -q 'spring.http.clients.connect-timeout' "$HOME/.cache/myfeeder-phase01/CLAUDE.committed.md" && grep -q 'reactor-netty-http' "$HOME/.cache/myfeeder-phase01/CLAUDE.committed.md" && grep -q 'HttpClientConfigurationTest' "$HOME/.cache/myfeeder-phase01/CLAUDE.committed.md" && grep -q 'Spring AI with Anthropic Claude (chat only)' "$HOME/.cache/myfeeder-phase01/CLAUDE.committed.md" && ! grep -q 'the JDK HttpClient default is' "$HOME/.cache/myfeeder-phase01/CLAUDE.committed.md"</automated>
    <fails_when>non-zero exit: a phase edit is missing from the committed CLAUDE.md, the stale JDK-client claim is still committed, or the user's uncommitted line-40 edit leaked into the commit (the committed line 40 no longer shows the original "chat only" text)</fails_when>
    <automated>git diff -U0 -- CLAUDE.md > "$HOME/.cache/myfeeder-phase01/claude-md-user-hunks.after.diff" && diff <(grep -v '^index ' "$HOME/.cache/myfeeder-phase01/claude-md-user-hunks.before.diff") <(grep -v '^index ' "$HOME/.cache/myfeeder-phase01/claude-md-user-hunks.after.diff")</automated>
    <fails_when>diff prints any line or exits non-zero: the user's uncommitted hunks changed, or a phase edit remains unstaged in the working copy</fails_when>
    <automated>git show --name-only --format= "$(git log -1 --format=%H -- CLAUDE.md)" > "$HOME/.cache/myfeeder-phase01/docs-commit-files.txt" && grep -qx 'CLAUDE.md' "$HOME/.cache/myfeeder-phase01/docs-commit-files.txt" && ! grep -q '^\.claude/' "$HOME/.cache/myfeeder-phase01/docs-commit-files.txt"</automated>
    <fails_when>non-zero exit: the docs commit does not contain CLAUDE.md or includes a .claude/ path</fails_when>
  </verify>
  <acceptance_criteria>
    - The committed CLAUDE.md contains `Spring Boot 4.0.8 feed aggregator`, `Spring Boot 4.0.8 with Spring MVC`, `spring.http.clients.connect-timeout`, `reactor-netty-http` and `HttpClientConfigurationTest`. It does not contain `the JDK HttpClient default is`.
    - The committed line 40 still reads `- **AI**: Spring AI with Anthropic Claude (chat only)`, the HEAD text. The user's rewording of that line stays uncommitted in the working copy.
    - Ignoring `index` lines, `git diff -U0 -- CLAUDE.md` after the commit is identical to the pre-task snapshot.
    - If `.claude/CLAUDE.md` was modified before this task, it is still modified afterwards, and no commit in this plan lists any `.claude/` path.
  </acceptance_criteria>
  <done>Root CLAUDE.md documents the upgraded stack, the Reactor Netty transport and the bound timeout keys in one docs commit, and the user's unrelated uncommitted edits are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maven Central → Gradle build | New BOM lines pull new versions of every managed transitive jar into the build |
| App → remote feed servers (outbound HTTP) | Untrusted remote hosts. The transport and timeouts decide how a slow or hostile server can affect scheduler threads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Gradle dependency resolution (build.gradle.kts BOM bump) | medium | mitigate | Only the 3 BOM version strings change. The one new direct declaration (reactor-netty-http) was already on the production classpath transitively and gets its version from the Boot BOM. Task 1 reviews the resolved tree (4.0.8 / 2.0.1 / 5.0.3, no webflux, no 4.1.x Boot artifact). |
| T-01-02 | Denial of Service | Outbound RestClient timeouts (application.yaml) | medium | mitigate | D-02 binds connect 5s / read 30s under spring.http.clients.*. Before this, a stalled feed could hold a scheduler thread forever. HttpClientConfigurationTest.outboundTimeoutsFromMainApplicationYamlBind guards the key names. |
| T-01-03 | Elevation of Privilege | SSRF guard on feed fetches (FeedFetcher / FeedUrlValidator) | high | mitigate | FeedUrlValidator runs before any request, whichever request factory is in use. D-01 keeps the transport unchanged. FeedFetcherTest and FeedUrlValidatorTest run in the Task 1 full suite and must pass. |
| T-01-04 | Tampering | Upgraded transitive libraries (Jackson 3.1.5, Spring Cloud Commons 5.0.3) | low | accept | These patch/minor bumps fix published CVEs (jackson-core GHSA-72hv-8253-57qq, Spring Cloud Commons CVE-2026-59284). The full test suite is the regression gate. No further control is needed at ASVS L1. |
</threat_model>

<verification>
- `grep` checks in Task 1 confirm the three target versions, the version-less reactor-netty-http line, and axion-release 1.21.1 / Gradle wrapper 9.4.1 unchanged (D-03).
- The resolved runtimeClasspath is on 4.0.8 / 2.0.1 / 5.0.3, with no Boot artifact on 4.1.x and no spring-webflux.
- The full Docker-backed suite (`DOCKER_HOST=unix:///Users/scottb/.docker/run/docker.sock ./gradlew cleanTest test -x npmBuild -x npmInstall`) is green, and its skipped count matches the recorded baseline.
- HttpClientConfigurationTest (both tests) is green and was seen RED before each fix.
- There are three commits (bump, D-02 fix, docs), and the D-02 commit is separate from the bump.
- The root CLAUDE.md docs commit contains only phase lines. The user's hunks are unchanged and uncommitted.
</verification>

<success_criteria>
- UPG-01 (backend half): the backend is on Spring Boot 4.0.8, Spring AI BOM 2.0.1 and Spring Cloud 2025.1.3, and every backend test passes, including the Testcontainers classes.
- D-01: Reactor Netty is still the outbound transport, proven by a permanent test.
- D-02: the 5s/30s timeouts are bound, proven by a permanent test, and landed in a separate commit.
- D-03: axion-release, the Gradle wrapper and the non-BOM pinned libraries are untouched.
- Root CLAUDE.md is accurate for the version, the transport and the timeout keys.
</success_criteria>

## Artifacts this phase produces

| Artifact | Produced by | Notes |
|----------|-------------|-------|
| `build.gradle.kts` (Boot 4.0.8, Spring AI 2.0.1, Spring Cloud 2025.1.3, reactor-netty-http) | **01-01 (this plan)** | Version bump commit |
| `src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java` | **01-01 (this plan)** | New permanent regression test (D-01/D-02) |
| `src/main/resources/application.yaml` (`spring.http.clients.*`) | **01-01 (this plan)** | Separate D-02 commit |
| `CLAUDE.md` (root) version/transport/timeout lines | **01-01 (this plan)** | Docs commit, phase lines only |
| `src/main/frontend/package.json`, `src/main/frontend/package-lock.json` | 01-02 | In-major npm refresh (D-04) |
| `.planning/codebase/STACK.md` | 01-03 | Resolved versions after both wave-1 plans |
| Merge commit on `main` (`--no-ff` of `sbartram/main`) | 01-03 | Local, unpushed until 01-04 approval |
| Git tag `v0.1.24`, image `registry.bartram.org/bartram/myfeeder:0.1.24`, Helm release revision on k3s | 01-04 | Outward-facing; gated by blocking-human approval (D-05) |
| Evidence under `$HOME/.cache/myfeeder-phase01/` (baseline test totals, dependency tree, CLAUDE.md hunk snapshots) | 01-01 (this plan) plus later plans | Outside the repo on purpose: the repo is public |

New symbols created by this plan (for source-grounding / drift exclusion):
- File `src/test/java/org/bartram/myfeeder/config/HttpClientConfigurationTest.java` in the new test package directory `src/test/java/org/bartram/myfeeder/config/`
- Class `org.bartram.myfeeder.config.HttpClientConfigurationTest` (package-private test class)
- Test method `outboundTransportIsReactorNetty()`
- Test method `outboundTimeoutsFromMainApplicationYamlBind()`
- Gradle dependency declaration `implementation("io.projectreactor.netty:reactor-netty-http")` (no version; Boot-BOM managed)
- Config keys `spring.http.clients.connect-timeout` / `spring.http.clients.read-timeout` in `src/main/resources/application.yaml` (renamed from the deprecated singular prefix)
- Evidence files `backend-baseline.txt`, `runtime-deps.txt`, `claude-md-user-hunks.before.diff`, `claude-md-user-hunks.after.diff`, `CLAUDE.head.md`, `CLAUDE.staged.md`, `CLAUDE.committed.md`, `docs-commit-files.txt` under `$HOME/.cache/myfeeder-phase01/`

## Flagged Assumptions (edge probe)

None of this plan's edge-probe rows are unresolved. UPG-01 adjacency, empty and ordering are resolved as explicit truths above. The UPG-02 and UPG-03 unclassified rows are carried as flagged assumptions in plans 01-02 and 01-04.

## Canon-referral breadcrumbs (prohibition probe)

- A weakened SSRF guard after the transport change is canon security, covered by /gsd-secure-phase and T-01-03. It is not minted as a prohibition here.

<output>
Create `.planning/phases/01-dependency-upgrade/01-01-SUMMARY.md` when done. Include the baseline and upgraded test totals, the RED/GREEN evidence for both HttpClientConfigurationTest methods, the three commit SHAs, and the resolved reactor-netty-http version.
</output>
