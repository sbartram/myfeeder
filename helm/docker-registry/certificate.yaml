apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: registry-bartram-org-tls
  namespace: docker-registry
spec:
  secretName: registry-bartram-org-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - registry.bartram.org
